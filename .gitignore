__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
data/raw/*
!data/raw/.gitkeep
!data/raw/README.md
# derived artifacts: articles.jsonl (from ingest) and embeddings.npz
# (model-specific, ~1 MB, rebuilt by `ablate` from the corpus fingerprint)
data/processed/*
!data/processed/.gitkeep
runs/
.env
!data/raw/SOURCE.txt
# P1 demo layer (ADR-023): uploaded documents, their chunks and embeddings
data/app/
# local model weights live in the project but are never committed
models/
# a Hugging Face cache written into the working directory by an empty HF_HUB_CACHE
models--*/
