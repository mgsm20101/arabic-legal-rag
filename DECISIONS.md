# DECISIONS — arabic-legal-rag

سجل قرارات معمارية. كل قرار: السياق، القرار، **البدائل المرفوضة**، و**شرط إعادة النظر**.
يُكتب لحظة القرار، مش دفعة واحدة في الآخر.

---

## ADR-001 — مجموعة التقييم تُكتب قبل أي كود استرجاع
**2026-09-07 · مقبول**

**السياق:** الترتيب الشائع (ابنِ ← قِس) بينتج أسئلة مفصّلة على النظام اللي اتبنى، فالرقم بيقيس نفسه.

**القرار:** أول ملف في الريبو هو `evals/retrieval/questions.jsonl`. ممنوع كتابة سطر استرجاع قبل ما 20 سؤال على الأقل تبقى مكتوبة ومقفولة.

**البدائل المرفوضة:**
- *بناء baseline أولاً ثم اشتقاق الأسئلة من فشله* — بينتج مجموعة متحيزة لنقاط ضعف معروفة، وبيخفي الفشل اللي محدش فكّر فيه.
- *استخدام مجموعة أسئلة مولّدة بـ LLM* — الأسئلة بتطلع بصياغة النص نفسه، فالاسترجاع بيبقى سهل بشكل كاذب.

**شرط إعادة النظر:** لو دخل متن تاني بحجم كبير (>500 مادة)، يُعاد النظر في توليد نصف الأسئلة آلياً **بشرط** مراجعة بشرية 100% وتوسيم مصدر كل سؤال.

---

## ADR-002 — التقطيع بالمادة، مش بنافذة توكن
**2026-09-07 · مقبول**

**السياق:** التقطيع الافتراضي في أغلب أدوات RAG نافذة ثابتة (512 توكن) بتداخل.

**القرار:** وحدة التقطيع = المادة القانونية كاملة، مهما كان طولها.

**السبب:** كل إجابة في الـ ground truth مفتاحها رقم مادة. نافذة ثابتة بتقطع المادة نصين، فيبقى السؤال "استرجع المادة 7 صح؟" بلا معنى. وكمان المادة القانونية وحدة دلالية مكتفية بذاتها بحكم الصياغة التشريعية.

**البدائل المرفوضة:**
- *512-token window* — يكسر مفتاح التحقق (فوق).
- *تقطيع بالفقرة داخل المادة* — يرفع الدقة نظرياً لكن يعقّد الـ ground truth بلا عائد مقاس. **شرط إعادة النظر:** لو ظهر إن المواد الطويلة (>1500 حرف) هي مصدر أغلب فشل الاسترجاع في Error Analysis.

---

## ADR-003 — متن واحد فقط في M1
**2026-09-07 · مقبول**

**القرار:** قانون 151/2020 وحده. قانون العمل كان في خطة v1.0 واتشال.

**السبب:** متنين = ضعف عمل الـ ground truth، وبيضيف بُعد "أي قانون؟" للسؤال قبل ما البُعد الأساسي (أي مادة؟) يبقى مقاساً.

**شرط إعادة النظر:** بعد تسليم M1 بأرقام مستقرة، إضافة متن تاني تبقى امتداد طبيعي وبيقيس قدرة مهمة (توجيه بين متون).

---

## ADR-004 — فصل تطبيع البحث عن تطبيع التقييم
**2026-09-07 · مقبول**

**السياق:** العربية فيها تنويعات إملائية مشروعة (أ/إ/آ/ا، ة/ه، ى/ي) والكاتب المصري بيخلط بينها.

**القرار:** دالتان منفصلتان — `search_normalize` (شرسة، للفهرسة والاستعلام) و `evaluation_normalize` (محافظة، للمقارنة بالمرجع).

**السبب:** استخدام التطبيع الشرس وقت التقييم بيخلي إجابة غلط تبان صح، فبيضخّم النتيجة. القاعدة: التطبيع اللي بيساعد الاسترجاع هو نفسه اللي بيغش المقياس.

*(نفس الدرس اتعلّم في مشروع استخراج النماذج — فصل قيمة المراجعة عن قيمة البحث.)*

**البديل المرفوض:** *دالة واحدة للاتنين* — أبسط، لكن تكلفتها رقم متفائل كذّاب.

**شرط إعادة النظر:** لا. الفصل ده دائم.

---

## ADR-005 — التحقق الآلي من مراجع المواد إلزامي
**2026-09-07 · مقبول**

**السياق:** الـ ground truth الأولي اتكتب من مصادر ثانوية على الويب، مش من الجريدة الرسمية. احتمال خطأ رقم المادة حقيقي.

**القرار:** كل سؤال بيبدأ `ref_status: "unverified"`. `make verify-refs` بيتأكد إن المادة موجودة في المتن المُستوعب وإن الكلمات المفتاحية المتوقعة فيها فعلاً. ممنوع أي قياس يستهلك سؤال `unverified`.

**السبب:** خطأ في المرجع مش بيظهر كخطأ — بيظهر كنتيجة استرجاع سيئة، فبتتصلح في المكان الغلط.

**البديل المرفوض:** *مراجعة يدوية فقط* — لا تتكرر مع كل تحديث للمتن، وبتنهار عند 60 سؤال.

---

## ADR-006 — مصدر المتن: PDF رسمي يُنزَّل يدوياً، مش كشط ويب
**2026-09-07 · مقبول**

**السياق:** الصفحات العامة اللي فيها نص القانون بتقدّمه ملخّصاً أو مجزّأً، والاستخراج الآلي منها بيرجع إعادة صياغة مش نصاً حرفياً.

**القرار:** النص بيتاخد من ملف PDF رسمي (الجريدة الرسمية أو منشورات قانونية) يُنزَّل مرة واحدة ويتحط في `data/raw/`، وبيتوسّم مصدره وتاريخه في `data/raw/README.md`.

**السبب:** إعادة الصياغة تفسد المتن والـ ground truth معاً، ومش هتبان في أي مقياس.

**البديل المرفوض:** *كشط المواد من مواقع قانونية* — أسرع، لكن بلا ضمان حرفية النص.

**شرط إعادة النظر:** لو اتوفر مصدر نصي رسمي (API أو ملف نصي من جهة رسمية) بترخيص واضح.

> **حالة القرار:** اتعدّل بـ ADR-007 (2026-09-07) ورجع ساري بـ ADR-012 (2026-09-08). ده القرار النافذ دلوقتي.

---

## ADR-007 — مصدر المتن: `dataflare/egypt-legal-corpus` بدل تنزيل PDF يدوي
**2026-09-07 · ~~مقبول~~ · مُلغى بـ ADR-012 (2026-09-08) · ADR-006 رجع ساري**

**السياق:** ADR-006 كان بيفرض تنزيل PDF رسمي يدوياً لأن الصفحات العامة بترجع ملخصات. ده كان **معطّل حقيقي** للمشروع. اتعرض بديل: مجموعة بيانات على Hugging Face، رخصة MIT، 2,434 صف (صف = قانون كامل)، حقول `law_name` / `categories` / `text` / `tokens`، و~25 مليون توكن، ونص المواد جواها بعناوين من نوع «مادة 1».

**القرار:** المجموعة دي تبقى **مصدر النص الخام**، مش المتن. النص بيعدّي على نفس المقسّم ونفس التحقق البنيوي زي أي PDF، وبيتقفل على **قانون واحد** في M1 (ADR-003 لسه ساري).

**السبب:** بيشيل المعطّل بدون ما يشيل الصرامة — التقطيع والتحقق وتثبيت المراجع كلهم في مكانهم.

**شرط إلزامي مصاحب (فحص أمانة المتن):** قبل نشر أي رقم، تُقارن **10 مواد عشوائية** من المتن المُستوعَب بالنص الرسمي، وتُسجَّل النتيجة في `EVAL.md` كقسم "Corpus fidelity". السبب: مصدر المجموعة موصوف بـ «extracted and processed» بدون تفصيل، ومعاينة الصفوف بتوحي إن النص **مُطبَّع مسبقاً** (همزات ساقطة). لو ده اتأكد، فده مش مجرد ملاحظة — **ADR-004 بيتأثر**: لو المصدر نفسه ضيّع تمييز الهمزة والتاء المربوطة، يبقى تطبيع التقييم المحافظ فقد جزءاً من معناه، ولازم يتقال صراحةً في `EVAL.md` بدل ما يتقدّم كأنه صارم.

**البدائل المرفوضة:**
- *استبدال التحقق بالثقة في المجموعة* — مصدر مجهول الأمانة أساس فاسد لمشروع كل قيمته إنه مقيس.
- *استيعاب الـ 2,434 قانون كلهم في M1* — يفجّر ميزانية 16 ساعة ويغيّر السؤال البحثي قبل ما الأساسي يتقاس.

**شرط إعادة النظر:** لو فحص الأمانة فشل على أكتر من مادة من العشرة → نرجع لـ ADR-006 (PDF رسمي) للقانون ده تحديداً.

---

## ADR-008 — `fr3on/eg-legal-qa` مرفوضة كمجموعة تقييم أساسية، مقبولة كمجموعة ثانوية
**2026-09-07 · مقبول**

**السياق:** مجموعة 5,230 سؤال/إجابة، رخصة Apache-2.0، على قانون الإجراءات الجنائية، والـ metadata فيها `article_number` و`document_title` — يعني مفاتيح تحقق جاهزة.

**القرار:** **مش** مجموعة التقييم الأساسية. تُستخدم كمجموعة **ثانوية** لاختبار التعميم بعد تجميد التكوين على المجموعة المكتوبة يدوياً.

**السبب — أربع مشاكل، أولها قاتل:**
1. **الدائرية:** الأسئلة مولَّدة من نص المواد نفسها، فبتشترك معاها في الألفاظ. الاسترجاع المعجمي (BM25) هيبان ممتاز لسبب مصطنع. ده **حرفياً** البديل اللي اتسجّل مرفوضاً في ADR-001 بنفس السبب.
2. **مفيش فئة امتناع:** مجموعة مولّدة لكل مادة ما بتحتويش أسئلة بلا إجابة. محور الامتناع كله (25% من مجموعتنا) غايب.
3. **آثار توليد ظاهرة:** الكارت نفسه فيه إجابة متكررة حرفياً («الحقوق والالتزامات المنصوص عليها: الحقوق والالتزامات المنصوص عليها») — ناتج متدهور، مش مرجع صالح للتوليد.
4. **قانون مختلف:** إجراءات جنائية، مش القانون اللي المتن عليه.

**قيمتها الحقيقية (وهي كبيرة):** الفرق بين النتيجة عليها والنتيجة على المجموعة المكتوبة يدوياً **هو النتيجة**. لو الاسترجاع 0.9 على المولّدة و0.6 على اليدوية، ده اكتشاف يستحق قسم في `EVAL.md` وبيوصّل في المقابلة حاجة أهم من رقم مرتفع: إنك بتعرف إمتى الرقم كذّاب.

**شرط الاستخدام:** تُشغَّل **مرة واحدة بعد التجميد**، وتُنشر أرقامها موسومة بـ "generated set — لا تُقارن بأرقام المجموعة اليدوية إلا كفجوة".

**شرط إعادة النظر لترقيتها لأساسية:** لا. مجموعة مولّدة آلياً ما تبقاش المرجع الأساسي في مشروع كل ادعائه إنه بيقيس صح.

---

## ADR-009 — حد أقصى للتداخل اللفظي بين السؤال والمادة اللي بتجاوبه
**2026-09-07 · مقبول**

**السياق:** ADR-008 رفض مجموعة أسئلة عامة لأنها مولّدة من نص المواد، فبتشترك معاها في الألفاظ والاسترجاع المعجمي بيبان ممتاز لسبب مصطنع. نفس العيب ممكن يدخل مجموعتنا اليدوية بالصدفة — أسهل طريقة تكتب بيها سؤال هي إنك تعيد صياغة المادة.

**القرار:** كل سؤال بيتقاس عليه **containment**: نسبة كلمات السؤال المحتواة (بعد حذف كلمات الوظيفة) اللي موجودة في المادة المتوقعة. الحد `max_lexical_overlap` في `meta.json` (حالياً 0.55). فوق الحد → السؤال **ما يتختمش verified**، وبيتطبع مع الكلمات المنسوخة عشان تتعاد صياغته.

**ليه containment مش Jaccard:** المادة أطول من السؤال بمراتب، فالـ Jaccard هيفضل قريب من الصفر لكل الأسئلة وبيقيس طول المادة مش النسخ. الـ containment بيجاوب السؤال الصح: قد إيه من السؤال ده منقول من الفقرة؟

**تحقق عملي:** «خلال كام ساعة يجب إبلاغ المركز بخرق البيانات الشخصية؟» = 0.50 · نفس المعنى بصياغة المستخدم «لو حصل اختراق للداتا عندي أعمل إيه وفي خلال قد إيه؟» = 0.14.

**البدائل المرفوضة:**
- *مراجعة بالعين* — ما بتتكررش مع كل تعديل، وبتنهار عند 60 سؤال.
- *منع أي كلمة مشتركة* — مستحيل: أي سؤال قانوني لازم يسمّي الموضوع. الحد نسبي مش صفري.

**شرط إعادة النظر:** لو ظهر إن الحد بيرفض أسئلة سليمة (فئة multi_article بتستخدم أسماء الأدوار القانونية اضطراراً) → يترفع الحد لتلك الفئة تحديداً مع توثيق السبب، مش يتلغي.

---

## ADR-010 — ربط مجموعة الأسئلة بالقانون اللي اتكتبت عليه
**2026-09-07 · مقبول**

**السياق:** المتن بقى قابل للتبديل (ADR-007: أي قانون من 2,434). مفيش حاجة كانت بتمنع تشغيل أسئلة قانون حماية البيانات على متن قانون المرافعات.

**القرار:** `evals/retrieval/meta.json` بيحمل `corpus_law`، وكل من `evaluate` و`verify-refs` **بيرفضوا التشغيل** (exit 3) لو القانون المُستوعَب مش مطابق.

**السبب:** عدم التطابق بيطلع كنتيجة استرجاع ضعيفة — يعني بيتشخّص كمشكلة في النظام ويتصلح في المكان الغلط. ده نفس نمط الخطأ اللي ADR-005 اتعمل عشانه.

**حالة اليوم:** `corpus_law_status: TARGET_NOT_CONFIRMED` — لسه متأكدناش إن قانون 151/2020 موجود أصلاً في المجموعة. لو مش موجود، الأسئلة تتعاد كتابة على قانون موجود، مش تتشغّل كما هي.

---

## ADR-011 — تقشير السوابق المتصلة (clitics) في تطبيع البحث فقط
**2026-09-07 · مقبول**

**السياق:** العربية بتلزق حروف العطف (و/ف) وحروف الجر (ب/ك/ل) وأداة التعريف (ال) في أول الكلمة. فـ«البيانات» و«للبيانات» و«وبالبيانات» تلات رموز مختلفة لأي مُقسِّم بالمسافات. الأثر مش نظري: قِيس فعلياً إن تداخل سؤال منسوخ من مادة كان **0.50** قبل التقشير و**0.88** بعده — يعني المقياس كان بيقرا أقل من الحقيقة بالنص تقريباً، والـ BM25 كان بيخسر تطابقات لنفس السبب.

**القرار:** تقشير **مرة واحدة**، بأطول تطابق أولاً، داخل `search_normalize` فقط. `evaluation_normalize` ما بيقشّرش — حذف حرف جر بيغيّر المعنى القانوني.

**تفصيلة مهمة — «لل»:** ألف «ال» بتسقط بعد لام الجر («لـ» + «البيانات» = «للبيانات»)، فلازم قاعدة مستقلة؛ تقشير «ل» لوحدها بيسيب «لبيانات».

**ليه مرة واحدة مش تكراري:** التكرار بياكل الجذور — «البيانات» ← «بيانات» ← «يانات». والحد الأدنى (3 حروف متبقية) بيحمي «لبن» و«بيت».

**البدائل المرفوضة:**
- *مُجذِّر كامل (ISRI/Farasa)* — أدق نظرياً، بس بيضيف تبعية ثقيلة وبيجذّر بعنف على المصطلحات القانونية («المتحكم»/«المعالج» ممكن يتلموا على جذر واحد). **شرط إعادة النظر:** لو Error Analysis وضّح إن فشل الاسترجاع سببه تصريفي مش سوابق.
- *ما نعملش حاجة* — الأرقام فوق بتقول إن التكلفة مش مقبولة.

---

## ADR-012 — التراجع عن Hugging Face والرجوع للـ PDF الرسمي
**2026-09-08 · مقبول · يلغي ADR-007 · ADR-006 يرجع ساري**

**السياق:** ADR-007 استبدل تنزيل الـ PDF اليدوي بمجموعة `dataflare/egypt-legal-corpus` عشان يشيل المعطّل. المعطّل اتشال فعلاً، بس اتشال بتكلفة كان ADR-007 نفسه مسجّلها في شرط الأمانة بتاعه: المصدر موصوف بـ «extracted and processed» من غير تفصيل، والمعاينة بتوحي بنص **مُطبَّع مسبقاً** (همزات ساقطة). يعني المتن اللي المشروع كله قايم على إنه مقيس، مصدره طرف تالت مش معروف إيه اللي عمله في النص.

**القرار:** الـ PDF الرسمي يبقى **المصدر الوحيد** للمتن. الكود المتعلق بـ HF (`fetch.py`، `corpus_hf.py`، `tasks laws`، `fetch-corpus`، تبعيات `pyarrow` و`huggingface_hub`) **اتشال بالكامل** مش اتساب كـ fallback.

**السبب — تلاتة:**
1. **الأمانة مش قابلة للتفويض.** فحص الـ 10 مواد في ADR-007 كان بيقارن المجموعة بالـ PDF. لو الـ PDF هو المرجع اللي بيحكم، يبقى هو المصدر من الأول — الطبقة الوسيطة بتضيف احتمال فساد بدون ما تضيف معلومة.
2. **مسار واحد مش اتنين.** fallback يعني فرعين في `ingest`، وفرع بيشتغل من غير ما حد يقصد. النسخة اللي فاتت كانت بتفضّل الـ parquet تلقائياً لمجرد وجوده على القرص.
3. **التكلفة الحقيقية للتراجع صغيرة.** المقسّم والتحقق و`--law` والحارس كلهم شغالين زي ما هم — اللي اتغير هو من فين بييجي النص.

**اللي اتكسب في الطريق (مش اترمي):** `--law` كان اتولد من ضرورة اختيار قانون من 2,434. بقى **إلزامي في مسار الـ PDF كمان** — لأن `law_name` هو اللي حارس ADR-010 بيفحصه، والنسخة القديمة من مسار الـ PDF كانت بتسيبه فاضي، و`binding_problem` بترجع `None` على قايمة فاضية. يعني الحارس كان **ميت بصمت** في المسار ده من الأول. ده باج كان مستخبي ورا ADR-007.

**إضافة إلزامية — فحص الاستخراج:** استخراج العربي من PDF بيفشل بطرق معقولة الشكل (مفيش طبقة نص / خط مش مربوط بـ Unicode / ترتيب RTL متخربط)، وكلها كانت بتطلع «ingested 0 articles» — عرض بيوجّه الإصلاح للمقسّم وهو مكانش هو المشكلة. `extraction_problems` بيفحص التلاتة وبيوقف الاستيعاب بـ exit 2 قبل ما يكتب أي حاجة.

**البدائل المرفوضة:**
- *الإبقاء على HF كـ fallback اختياري* — مسارين للصيانة، والمسار «الاحتياطي» بيبقى هو الشغال بحكم إن الملف موجود. مرفوض صراحةً في القرار ده.
- *الاحتفاظ بـ `corpus_hf.py` كود ميت للرجوع ليه* — `git` بيعمل ده. كود ميت في الريبو بيتقري كأنه مدعوم.

**الثمن المقبول:** المعطّل رجع — المتن مش هيبقى موجود لحد ما الـ PDF يتنزّل يدوياً. ده مقبول لأن البديل هو رقم مبني على متن مش متحقق منه، وده أسوأ من مفيش رقم.

**شرط إعادة النظر:** لو جهة رسمية وفّرت النص بصيغة نصية (API أو ملف) بترخيص واضح وبإسناد للجريدة الرسمية → يُستخدم مباشرةً بدون PDF. مجموعة معاد نشرها من طرف تالت **مش** بتحقق الشرط ده.

---

## ADR-013 — إعادة بناء الترتيب المنطقي للعربي من الـ PDF، وقواعد التقطيع اللي طلعت منه
**2026-09-08 · مقبول · يكمّل ADR-012 و ADR-002**

**السياق:** بعد ما ADR-012 رجّع الـ PDF مصدراً وحيداً، أول تشغيل حقيقي على قانون 151/2020 كشف إن `pdfplumber.extract_text()` **مش صالح للعربي**. بيرجّع الحروف بترتيب الرسم (بصري) مش منطقي، فـ`البيانات` بتوصل `تانايبلا`. الخطورة مش إنه بيفشل — الخطورة إنه **بينجح شكلياً**: الحروف عربية سليمة، والمقسّم بس مش بيلاقي `مادة`، والتشغيل بينتهي بـ «ingested 0 articles» — عرض بيوجّه الإصلاح للمقسّم وهو مكانش هو.

**القرار:** `src/legalrag/pdf_text.py` — إعادة بناء على مستوى الـ glyph، وهي المسار الوحيد لقراءة الـ PDF في `ingest`.

**أربع تفاصيل بتفصل إعادة بناء صحيحة عن واحدة «شكلها مظبوط» — كلها مقيسة مش مفترضة:**

1. **اعكس الـ glyphs مش السلسلة.** ligature زي `لا` / `لأ` / `لإ` هو glyph **واحد** بيتحوّل لحرفين **بترتيب منطقي أصلاً**. `reversed(text)` بيفكّه لـ `ال` وبيفسد كل كلمة فيها لام-ألف.
2. **رتّب بمركز الـ glyph مش بـ `x0`.** الـ `ر` ليها side bearing واسع، فـ`x0` بتاعها بتقع يسار الـ `ا` اللي قبلها بصرياً. الترتيب بـ`x0` قلب الزوج: `إجراءات` بقت `إجارءات`، `الآراء` بقت `الآارء`، `الجغرافي` بقت `الجغارفي`. الترتيب بـ`(x0+x1)/2` **صفّر** الأخطاء دي (36 كلمة صح / 0 غلط بعد التغيير).
3. **الأرقام العربية-الهندية مش حروف.** `٠-٩` و`۰-۹` جوه نطاق يونيكود العربي، فأي اختبار بالنطاق بيعتبرهم حروف **ويعكسهم**. النتيجة: `مادة (٢٥)` بقت `مادة (٥٢)`، و`٧٢ ساعة` كانت هتبقى `٢٧ ساعة`. في متن كل ground truth فيه مربوط بأرقام مواد ومُهَل، ده فساد **صامت وقاتل**. اتأكد بالعمود الإنجليزي: `مادة( ٧٢ (27) )` — الترجمة بتقول 27.
4. **افصل الأعمدة قبل أي حاجة.** النسخ المتداولة ثنائية اللغة. إسقاط **الحروف** اللاتينية مش كفاية: أرقام وأقواس عمود الترجمة بتفضل وتقع في سطر العربي، فـ`مادة (1)` بتوصل `مادة (1)  )1(` — رقمين لمادة واحدة. وكمان: **الأقواس محتاجة mirroring**، وده مكانش باين غير بعد فصل الأعمدة — قوس الترجمة السليم كان بيخلي قوس العمود العربي المقلوب يبان صح بالصدفة.

**تفصيلتان اتكشفوا وقت التشغيل، والاتنين كانوا هيعدّوا بصمت:**

- **تجميع السطور بالفجوة مش بالقسمة.** `round(bottom/tol)` بيقسم السطر لما الـ glyphs تقع على حدّي دلوين: في صفحة 4، خطوط 623.5/623.7 في دلو و624.0 في اللي بعده، فحرفين من `القانون` اترفعوا لسطر لوحدهم والكلمة فضلت `القانو` في المتن. المقاس: الاهتزاز داخل السطر ≤0.5pt، وتباعد السطور ≥17.5pt — الفصل واسع، والقسمة هي اللي كانت بتضيّعه. الحل: تجميع بالفجوة، والحد **يُشتق من المستند** (45% من وسيط تباعد سطوره) بدل رقم مفترض.
- **علامات التنوين بترجع كحروف.** الخط بيربط glyph التنوين مرة صح (`أولاً` ← `ً`) ومرة غلط (`فعلاً` ← `ا` تانية). الألف الزيادة **بتنجو من `strip_tashkeel`** لأنها حرف مش حركة. الفرق الوحيد الباقي هندسي: العلامات بترتفع 6.5pt فوق خط أساس 12pt (0.54 من حجم الخط)، وأكبر انحراف شريف — glyph مسافة — 1.7pt (0.17). القطع عند 0.35.

**قواعد تقطيع اتغيرت نتيجة النص الحقيقي (بتعدّل ADR-002):**

- **الترويسة لازم تشغل سطرها لوحدها.** `المادة (21) من هذا القانون ...` بتبدأ سطراً ملفوفاً وهي **إحالة مش ترويسة**؛ من غير الشرط ده فتحت مادة 21 تانية وقطعت المادة 21 الحقيقية نصين. المقاس على الـ PDF: **56 سطر ترويسة خالص** مقابل **إحالة واحدة**.
- **إعادة بدء الترقيم بتفصل قانون الإصدار عن القانون المرافق.** الـ PDF ده بيرقّم مواد الإصدار `المادة (1)` مش `المادة الأولى`، فنمط الترتيب في ADR-002 مبيلقطهاش والكتابين بيتصادموا (تكرار 1..7). الإشارة هي **إعادة البدء**: أول ترويسة رقمها مش أكبر من اللي قبلها. اللي قبلها = إصدار. مستند مبيعيدش البدء بيفضل زي ما هو.

**النتيجة المقيسة:** 7 مواد إصدار + 49 مادة قانون = **الهيكل المنشور بالظبط** لقانون 151/2020، و`structure OK`. الباقي من التشوّه: **8 كلمات** فيها ألف مكررة من 35,071 حرف (مسجّلة في `SOURCE.txt`).

**البدائل المرفوضة:**
- *`arabic_reshaper` + `python-bidi`* — الاتنين بيحوّلوا **منطقي ← بصري**، وإحنا محتاجين العكس. مش موجود عكسهم كمكتبة.
- *عكس السلسلة النصية بعد `extract_text()`* — أبسط سطرين، وبيفسد كل لام-ألف وكل رقم. جُرّب فعلاً وأنتج `الإخلال` ← `اإلخالل`.
- *OCR للنسخة الرسمية بدل إصلاح الاستخراج* — الصح مبدئياً (النسخة الرسمية هي المرجع)، لكن الجريدة الرسمية ممسوحة ضوئياً و`tesseract` مش متثبت. **مؤجل، مش مرفوض** — والفحص لسه مفتوح في `SOURCE.txt`.

**شرط إعادة النظر:** لو دخل PDF من مصدر تاني وفشل فيه الاستخراج، تُقاس الأرقام (نسبة العربي، تباعد السطور، ارتفاع العلامات) على المستند الجديد قبل تعديل أي ثابت — الثوابت دي مقيسة على مستند واحد، مش قوانين طبيعة.

