# DECISIONS — arabic-legal-rag

سجل قرارات معمارية. كل قرار: السياق، القرار، **البدائل المرفوضة**، و**شرط إعادة النظر**.
يُكتب لحظة القرار، مش دفعة واحدة في الآخر.

---

## ADR-001 — مجموعة التقييم تُكتب قبل أي كود استرجاع
**2026-09-07 · مقبول**

**السياق:** الترتيب الشائع (ابنِ ← قِس) بينتج أسئلة مفصّلة على النظام اللي اتبنى، فالرقم بيقيس نفسه.

**القرار:** أول ملف في الريبو هو `evals/retrieval/questions.jsonl`. ممنوع كتابة سطر استرجاع قبل ما 20 سؤال على الأقل تبقى مكتوبة ومقفولة.

**البدائل المرفوضة:**
- *بناء baseline أولاً ثم اشتقاق الأسئلة من فشله* — بينتج مجموعة متحيزة لنقاط ضعف معروفة، وبيخفي الفشل اللي محدش فكّر فيه.
- *استخدام مجموعة أسئلة مولّدة بـ LLM* — الأسئلة بتطلع بصياغة النص نفسه، فالاسترجاع بيبقى سهل بشكل كاذب.

**شرط إعادة النظر:** لو دخل متن تاني بحجم كبير (>500 مادة)، يُعاد النظر في توليد نصف الأسئلة آلياً **بشرط** مراجعة بشرية 100% وتوسيم مصدر كل سؤال.

---

## ADR-002 — التقطيع بالمادة، مش بنافذة توكن
**2026-09-07 · مقبول**

**السياق:** التقطيع الافتراضي في أغلب أدوات RAG نافذة ثابتة (512 توكن) بتداخل.

**القرار:** وحدة التقطيع = المادة القانونية كاملة، مهما كان طولها.

**السبب:** كل إجابة في الـ ground truth مفتاحها رقم مادة. نافذة ثابتة بتقطع المادة نصين، فيبقى السؤال "استرجع المادة 7 صح؟" بلا معنى. وكمان المادة القانونية وحدة دلالية مكتفية بذاتها بحكم الصياغة التشريعية.

**البدائل المرفوضة:**
- *512-token window* — يكسر مفتاح التحقق (فوق).
- *تقطيع بالفقرة داخل المادة* — يرفع الدقة نظرياً لكن يعقّد الـ ground truth بلا عائد مقاس. **شرط إعادة النظر:** لو ظهر إن المواد الطويلة (>1500 حرف) هي مصدر أغلب فشل الاسترجاع في Error Analysis.

---

## ADR-003 — متن واحد فقط في M1
**2026-09-07 · مقبول**

**القرار:** قانون 151/2020 وحده. قانون العمل كان في خطة v1.0 واتشال.

**السبب:** متنين = ضعف عمل الـ ground truth، وبيضيف بُعد "أي قانون؟" للسؤال قبل ما البُعد الأساسي (أي مادة؟) يبقى مقاساً.

**شرط إعادة النظر:** بعد تسليم M1 بأرقام مستقرة، إضافة متن تاني تبقى امتداد طبيعي وبيقيس قدرة مهمة (توجيه بين متون).

---

## ADR-004 — فصل تطبيع البحث عن تطبيع التقييم
**2026-09-07 · مقبول**

**السياق:** العربية فيها تنويعات إملائية مشروعة (أ/إ/آ/ا، ة/ه، ى/ي) والكاتب المصري بيخلط بينها.

**القرار:** دالتان منفصلتان — `search_normalize` (شرسة، للفهرسة والاستعلام) و `evaluation_normalize` (محافظة، للمقارنة بالمرجع).

**السبب:** استخدام التطبيع الشرس وقت التقييم بيخلي إجابة غلط تبان صح، فبيضخّم النتيجة. القاعدة: التطبيع اللي بيساعد الاسترجاع هو نفسه اللي بيغش المقياس.

*(نفس الدرس اتعلّم في مشروع استخراج النماذج — فصل قيمة المراجعة عن قيمة البحث.)*

**البديل المرفوض:** *دالة واحدة للاتنين* — أبسط، لكن تكلفتها رقم متفائل كذّاب.

**شرط إعادة النظر:** لا. الفصل ده دائم.

---

## ADR-005 — التحقق الآلي من مراجع المواد إلزامي
**2026-09-07 · مقبول**

**السياق:** الـ ground truth الأولي اتكتب من مصادر ثانوية على الويب، مش من الجريدة الرسمية. احتمال خطأ رقم المادة حقيقي.

**القرار:** كل سؤال بيبدأ `ref_status: "unverified"`. `make verify-refs` بيتأكد إن المادة موجودة في المتن المُستوعب وإن الكلمات المفتاحية المتوقعة فيها فعلاً. ممنوع أي قياس يستهلك سؤال `unverified`.

**السبب:** خطأ في المرجع مش بيظهر كخطأ — بيظهر كنتيجة استرجاع سيئة، فبتتصلح في المكان الغلط.

**البديل المرفوض:** *مراجعة يدوية فقط* — لا تتكرر مع كل تحديث للمتن، وبتنهار عند 60 سؤال.

---

## ADR-006 — مصدر المتن: PDF رسمي يُنزَّل يدوياً، مش كشط ويب
**2026-09-07 · مقبول**

**السياق:** الصفحات العامة اللي فيها نص القانون بتقدّمه ملخّصاً أو مجزّأً، والاستخراج الآلي منها بيرجع إعادة صياغة مش نصاً حرفياً.

**القرار:** النص بيتاخد من ملف PDF رسمي (الجريدة الرسمية أو منشورات قانونية) يُنزَّل مرة واحدة ويتحط في `data/raw/`، وبيتوسّم مصدره وتاريخه في `data/raw/README.md`.

**السبب:** إعادة الصياغة تفسد المتن والـ ground truth معاً، ومش هتبان في أي مقياس.

**البديل المرفوض:** *كشط المواد من مواقع قانونية* — أسرع، لكن بلا ضمان حرفية النص.

**شرط إعادة النظر:** لو اتوفر مصدر نصي رسمي (API أو ملف نصي من جهة رسمية) بترخيص واضح.

> **حالة القرار:** اتعدّل بـ ADR-007 (2026-09-07) ورجع ساري بـ ADR-012 (2026-09-08). ده القرار النافذ دلوقتي.

---

## ADR-007 — مصدر المتن: `dataflare/egypt-legal-corpus` بدل تنزيل PDF يدوي
**2026-09-07 · ~~مقبول~~ · مُلغى بـ ADR-012 (2026-09-08) · ADR-006 رجع ساري**

**السياق:** ADR-006 كان بيفرض تنزيل PDF رسمي يدوياً لأن الصفحات العامة بترجع ملخصات. ده كان **معطّل حقيقي** للمشروع. اتعرض بديل: مجموعة بيانات على Hugging Face، رخصة MIT، 2,434 صف (صف = قانون كامل)، حقول `law_name` / `categories` / `text` / `tokens`، و~25 مليون توكن، ونص المواد جواها بعناوين من نوع «مادة 1».

**القرار:** المجموعة دي تبقى **مصدر النص الخام**، مش المتن. النص بيعدّي على نفس المقسّم ونفس التحقق البنيوي زي أي PDF، وبيتقفل على **قانون واحد** في M1 (ADR-003 لسه ساري).

**السبب:** بيشيل المعطّل بدون ما يشيل الصرامة — التقطيع والتحقق وتثبيت المراجع كلهم في مكانهم.

**شرط إلزامي مصاحب (فحص أمانة المتن):** قبل نشر أي رقم، تُقارن **10 مواد عشوائية** من المتن المُستوعَب بالنص الرسمي، وتُسجَّل النتيجة في `EVAL.md` كقسم "Corpus fidelity". السبب: مصدر المجموعة موصوف بـ «extracted and processed» بدون تفصيل، ومعاينة الصفوف بتوحي إن النص **مُطبَّع مسبقاً** (همزات ساقطة). لو ده اتأكد، فده مش مجرد ملاحظة — **ADR-004 بيتأثر**: لو المصدر نفسه ضيّع تمييز الهمزة والتاء المربوطة، يبقى تطبيع التقييم المحافظ فقد جزءاً من معناه، ولازم يتقال صراحةً في `EVAL.md` بدل ما يتقدّم كأنه صارم.

**البدائل المرفوضة:**
- *استبدال التحقق بالثقة في المجموعة* — مصدر مجهول الأمانة أساس فاسد لمشروع كل قيمته إنه مقيس.
- *استيعاب الـ 2,434 قانون كلهم في M1* — يفجّر ميزانية 16 ساعة ويغيّر السؤال البحثي قبل ما الأساسي يتقاس.

**شرط إعادة النظر:** لو فحص الأمانة فشل على أكتر من مادة من العشرة → نرجع لـ ADR-006 (PDF رسمي) للقانون ده تحديداً.

---

## ADR-008 — `fr3on/eg-legal-qa` مرفوضة كمجموعة تقييم أساسية، مقبولة كمجموعة ثانوية
**2026-09-07 · مقبول**

**السياق:** مجموعة 5,230 سؤال/إجابة، رخصة Apache-2.0، على قانون الإجراءات الجنائية، والـ metadata فيها `article_number` و`document_title` — يعني مفاتيح تحقق جاهزة.

**القرار:** **مش** مجموعة التقييم الأساسية. تُستخدم كمجموعة **ثانوية** لاختبار التعميم بعد تجميد التكوين على المجموعة المكتوبة يدوياً.

**السبب — أربع مشاكل، أولها قاتل:**
1. **الدائرية:** الأسئلة مولَّدة من نص المواد نفسها، فبتشترك معاها في الألفاظ. الاسترجاع المعجمي (BM25) هيبان ممتاز لسبب مصطنع. ده **حرفياً** البديل اللي اتسجّل مرفوضاً في ADR-001 بنفس السبب.
2. **مفيش فئة امتناع:** مجموعة مولّدة لكل مادة ما بتحتويش أسئلة بلا إجابة. محور الامتناع كله (25% من مجموعتنا) غايب.
3. **آثار توليد ظاهرة:** الكارت نفسه فيه إجابة متكررة حرفياً («الحقوق والالتزامات المنصوص عليها: الحقوق والالتزامات المنصوص عليها») — ناتج متدهور، مش مرجع صالح للتوليد.
4. **قانون مختلف:** إجراءات جنائية، مش القانون اللي المتن عليه.

**قيمتها الحقيقية (وهي كبيرة):** الفرق بين النتيجة عليها والنتيجة على المجموعة المكتوبة يدوياً **هو النتيجة**. لو الاسترجاع 0.9 على المولّدة و0.6 على اليدوية، ده اكتشاف يستحق قسم في `EVAL.md` وبيوصّل في المقابلة حاجة أهم من رقم مرتفع: إنك بتعرف إمتى الرقم كذّاب.

**شرط الاستخدام:** تُشغَّل **مرة واحدة بعد التجميد**، وتُنشر أرقامها موسومة بـ "generated set — لا تُقارن بأرقام المجموعة اليدوية إلا كفجوة".

**شرط إعادة النظر لترقيتها لأساسية:** لا. مجموعة مولّدة آلياً ما تبقاش المرجع الأساسي في مشروع كل ادعائه إنه بيقيس صح.

---

## ADR-009 — حد أقصى للتداخل اللفظي بين السؤال والمادة اللي بتجاوبه
**2026-09-07 · مقبول**

**السياق:** ADR-008 رفض مجموعة أسئلة عامة لأنها مولّدة من نص المواد، فبتشترك معاها في الألفاظ والاسترجاع المعجمي بيبان ممتاز لسبب مصطنع. نفس العيب ممكن يدخل مجموعتنا اليدوية بالصدفة — أسهل طريقة تكتب بيها سؤال هي إنك تعيد صياغة المادة.

**القرار:** كل سؤال بيتقاس عليه **containment**: نسبة كلمات السؤال المحتواة (بعد حذف كلمات الوظيفة) اللي موجودة في المادة المتوقعة. الحد `max_lexical_overlap` في `meta.json` (حالياً 0.55). فوق الحد → السؤال **ما يتختمش verified**، وبيتطبع مع الكلمات المنسوخة عشان تتعاد صياغته.

**ليه containment مش Jaccard:** المادة أطول من السؤال بمراتب، فالـ Jaccard هيفضل قريب من الصفر لكل الأسئلة وبيقيس طول المادة مش النسخ. الـ containment بيجاوب السؤال الصح: قد إيه من السؤال ده منقول من الفقرة؟

**تحقق عملي:** «خلال كام ساعة يجب إبلاغ المركز بخرق البيانات الشخصية؟» = 0.50 · نفس المعنى بصياغة المستخدم «لو حصل اختراق للداتا عندي أعمل إيه وفي خلال قد إيه؟» = 0.14.

**البدائل المرفوضة:**
- *مراجعة بالعين* — ما بتتكررش مع كل تعديل، وبتنهار عند 60 سؤال.
- *منع أي كلمة مشتركة* — مستحيل: أي سؤال قانوني لازم يسمّي الموضوع. الحد نسبي مش صفري.

**شرط إعادة النظر:** لو ظهر إن الحد بيرفض أسئلة سليمة (فئة multi_article بتستخدم أسماء الأدوار القانونية اضطراراً) → يترفع الحد لتلك الفئة تحديداً مع توثيق السبب، مش يتلغي.

---

## ADR-010 — ربط مجموعة الأسئلة بالقانون اللي اتكتبت عليه
**2026-09-07 · مقبول**

**السياق:** المتن بقى قابل للتبديل (ADR-007: أي قانون من 2,434). مفيش حاجة كانت بتمنع تشغيل أسئلة قانون حماية البيانات على متن قانون المرافعات.

**القرار:** `evals/retrieval/meta.json` بيحمل `corpus_law`، وكل من `evaluate` و`verify-refs` **بيرفضوا التشغيل** (exit 3) لو القانون المُستوعَب مش مطابق.

**السبب:** عدم التطابق بيطلع كنتيجة استرجاع ضعيفة — يعني بيتشخّص كمشكلة في النظام ويتصلح في المكان الغلط. ده نفس نمط الخطأ اللي ADR-005 اتعمل عشانه.

**حالة اليوم:** `corpus_law_status: TARGET_NOT_CONFIRMED` — لسه متأكدناش إن قانون 151/2020 موجود أصلاً في المجموعة. لو مش موجود، الأسئلة تتعاد كتابة على قانون موجود، مش تتشغّل كما هي.

---

## ADR-011 — تقشير السوابق المتصلة (clitics) في تطبيع البحث فقط
**2026-09-07 · مقبول**

**السياق:** العربية بتلزق حروف العطف (و/ف) وحروف الجر (ب/ك/ل) وأداة التعريف (ال) في أول الكلمة. فـ«البيانات» و«للبيانات» و«وبالبيانات» تلات رموز مختلفة لأي مُقسِّم بالمسافات. الأثر مش نظري: قِيس فعلياً إن تداخل سؤال منسوخ من مادة كان **0.50** قبل التقشير و**0.88** بعده — يعني المقياس كان بيقرا أقل من الحقيقة بالنص تقريباً، والـ BM25 كان بيخسر تطابقات لنفس السبب.

**القرار:** تقشير **مرة واحدة**، بأطول تطابق أولاً، داخل `search_normalize` فقط. `evaluation_normalize` ما بيقشّرش — حذف حرف جر بيغيّر المعنى القانوني.

**تفصيلة مهمة — «لل»:** ألف «ال» بتسقط بعد لام الجر («لـ» + «البيانات» = «للبيانات»)، فلازم قاعدة مستقلة؛ تقشير «ل» لوحدها بيسيب «لبيانات».

**ليه مرة واحدة مش تكراري:** التكرار بياكل الجذور — «البيانات» ← «بيانات» ← «يانات». والحد الأدنى (3 حروف متبقية) بيحمي «لبن» و«بيت».

**البدائل المرفوضة:**
- *مُجذِّر كامل (ISRI/Farasa)* — أدق نظرياً، بس بيضيف تبعية ثقيلة وبيجذّر بعنف على المصطلحات القانونية («المتحكم»/«المعالج» ممكن يتلموا على جذر واحد). **شرط إعادة النظر:** لو Error Analysis وضّح إن فشل الاسترجاع سببه تصريفي مش سوابق.
- *ما نعملش حاجة* — الأرقام فوق بتقول إن التكلفة مش مقبولة.

---

## ADR-012 — التراجع عن Hugging Face والرجوع للـ PDF الرسمي
**2026-09-08 · مقبول · يلغي ADR-007 · ADR-006 يرجع ساري**

**السياق:** ADR-007 استبدل تنزيل الـ PDF اليدوي بمجموعة `dataflare/egypt-legal-corpus` عشان يشيل المعطّل. المعطّل اتشال فعلاً، بس اتشال بتكلفة كان ADR-007 نفسه مسجّلها في شرط الأمانة بتاعه: المصدر موصوف بـ «extracted and processed» من غير تفصيل، والمعاينة بتوحي بنص **مُطبَّع مسبقاً** (همزات ساقطة). يعني المتن اللي المشروع كله قايم على إنه مقيس، مصدره طرف تالت مش معروف إيه اللي عمله في النص.

**القرار:** الـ PDF الرسمي يبقى **المصدر الوحيد** للمتن. الكود المتعلق بـ HF (`fetch.py`، `corpus_hf.py`، `tasks laws`، `fetch-corpus`، تبعيات `pyarrow` و`huggingface_hub`) **اتشال بالكامل** مش اتساب كـ fallback.

**السبب — تلاتة:**
1. **الأمانة مش قابلة للتفويض.** فحص الـ 10 مواد في ADR-007 كان بيقارن المجموعة بالـ PDF. لو الـ PDF هو المرجع اللي بيحكم، يبقى هو المصدر من الأول — الطبقة الوسيطة بتضيف احتمال فساد بدون ما تضيف معلومة.
2. **مسار واحد مش اتنين.** fallback يعني فرعين في `ingest`، وفرع بيشتغل من غير ما حد يقصد. النسخة اللي فاتت كانت بتفضّل الـ parquet تلقائياً لمجرد وجوده على القرص.
3. **التكلفة الحقيقية للتراجع صغيرة.** المقسّم والتحقق و`--law` والحارس كلهم شغالين زي ما هم — اللي اتغير هو من فين بييجي النص.

**اللي اتكسب في الطريق (مش اترمي):** `--law` كان اتولد من ضرورة اختيار قانون من 2,434. بقى **إلزامي في مسار الـ PDF كمان** — لأن `law_name` هو اللي حارس ADR-010 بيفحصه، والنسخة القديمة من مسار الـ PDF كانت بتسيبه فاضي، و`binding_problem` بترجع `None` على قايمة فاضية. يعني الحارس كان **ميت بصمت** في المسار ده من الأول. ده باج كان مستخبي ورا ADR-007.

**إضافة إلزامية — فحص الاستخراج:** استخراج العربي من PDF بيفشل بطرق معقولة الشكل (مفيش طبقة نص / خط مش مربوط بـ Unicode / ترتيب RTL متخربط)، وكلها كانت بتطلع «ingested 0 articles» — عرض بيوجّه الإصلاح للمقسّم وهو مكانش هو المشكلة. `extraction_problems` بيفحص التلاتة وبيوقف الاستيعاب بـ exit 2 قبل ما يكتب أي حاجة.

**البدائل المرفوضة:**
- *الإبقاء على HF كـ fallback اختياري* — مسارين للصيانة، والمسار «الاحتياطي» بيبقى هو الشغال بحكم إن الملف موجود. مرفوض صراحةً في القرار ده.
- *الاحتفاظ بـ `corpus_hf.py` كود ميت للرجوع ليه* — `git` بيعمل ده. كود ميت في الريبو بيتقري كأنه مدعوم.

**الثمن المقبول:** المعطّل رجع — المتن مش هيبقى موجود لحد ما الـ PDF يتنزّل يدوياً. ده مقبول لأن البديل هو رقم مبني على متن مش متحقق منه، وده أسوأ من مفيش رقم.

**شرط إعادة النظر:** لو جهة رسمية وفّرت النص بصيغة نصية (API أو ملف) بترخيص واضح وبإسناد للجريدة الرسمية → يُستخدم مباشرةً بدون PDF. مجموعة معاد نشرها من طرف تالت **مش** بتحقق الشرط ده.

---

## ADR-013 — إعادة بناء الترتيب المنطقي للعربي من الـ PDF، وقواعد التقطيع اللي طلعت منه
**2026-09-08 · مقبول · يكمّل ADR-012 و ADR-002**

**السياق:** بعد ما ADR-012 رجّع الـ PDF مصدراً وحيداً، أول تشغيل حقيقي على قانون 151/2020 كشف إن `pdfplumber.extract_text()` **مش صالح للعربي**. بيرجّع الحروف بترتيب الرسم (بصري) مش منطقي، فـ`البيانات` بتوصل `تانايبلا`. الخطورة مش إنه بيفشل — الخطورة إنه **بينجح شكلياً**: الحروف عربية سليمة، والمقسّم بس مش بيلاقي `مادة`، والتشغيل بينتهي بـ «ingested 0 articles» — عرض بيوجّه الإصلاح للمقسّم وهو مكانش هو.

**القرار:** `src/legalrag/pdf_text.py` — إعادة بناء على مستوى الـ glyph، وهي المسار الوحيد لقراءة الـ PDF في `ingest`.

**أربع تفاصيل بتفصل إعادة بناء صحيحة عن واحدة «شكلها مظبوط» — كلها مقيسة مش مفترضة:**

1. **اعكس الـ glyphs مش السلسلة.** ligature زي `لا` / `لأ` / `لإ` هو glyph **واحد** بيتحوّل لحرفين **بترتيب منطقي أصلاً**. `reversed(text)` بيفكّه لـ `ال` وبيفسد كل كلمة فيها لام-ألف.
2. **رتّب بمركز الـ glyph مش بـ `x0`.** الـ `ر` ليها side bearing واسع، فـ`x0` بتاعها بتقع يسار الـ `ا` اللي قبلها بصرياً. الترتيب بـ`x0` قلب الزوج: `إجراءات` بقت `إجارءات`، `الآراء` بقت `الآارء`، `الجغرافي` بقت `الجغارفي`. الترتيب بـ`(x0+x1)/2` **صفّر** الأخطاء دي (36 كلمة صح / 0 غلط بعد التغيير).
3. **الأرقام العربية-الهندية مش حروف.** `٠-٩` و`۰-۹` جوه نطاق يونيكود العربي، فأي اختبار بالنطاق بيعتبرهم حروف **ويعكسهم**. النتيجة: `مادة (٢٥)` بقت `مادة (٥٢)`، و`٧٢ ساعة` كانت هتبقى `٢٧ ساعة`. في متن كل ground truth فيه مربوط بأرقام مواد ومُهَل، ده فساد **صامت وقاتل**. اتأكد بالعمود الإنجليزي: `مادة( ٧٢ (27) )` — الترجمة بتقول 27.
4. **افصل الأعمدة قبل أي حاجة.** النسخ المتداولة ثنائية اللغة. إسقاط **الحروف** اللاتينية مش كفاية: أرقام وأقواس عمود الترجمة بتفضل وتقع في سطر العربي، فـ`مادة (1)` بتوصل `مادة (1)  )1(` — رقمين لمادة واحدة. وكمان: **الأقواس محتاجة mirroring**، وده مكانش باين غير بعد فصل الأعمدة — قوس الترجمة السليم كان بيخلي قوس العمود العربي المقلوب يبان صح بالصدفة.

**تفصيلتان اتكشفوا وقت التشغيل، والاتنين كانوا هيعدّوا بصمت:**

- **تجميع السطور بالفجوة مش بالقسمة.** `round(bottom/tol)` بيقسم السطر لما الـ glyphs تقع على حدّي دلوين: في صفحة 4، خطوط 623.5/623.7 في دلو و624.0 في اللي بعده، فحرفين من `القانون` اترفعوا لسطر لوحدهم والكلمة فضلت `القانو` في المتن. المقاس: الاهتزاز داخل السطر ≤0.5pt، وتباعد السطور ≥17.5pt — الفصل واسع، والقسمة هي اللي كانت بتضيّعه. الحل: تجميع بالفجوة، والحد **يُشتق من المستند** (45% من وسيط تباعد سطوره) بدل رقم مفترض.
- **علامات التنوين بترجع كحروف.** الخط بيربط glyph التنوين مرة صح (`أولاً` ← `ً`) ومرة غلط (`فعلاً` ← `ا` تانية). الألف الزيادة **بتنجو من `strip_tashkeel`** لأنها حرف مش حركة. الفرق الوحيد الباقي هندسي: العلامات بترتفع 6.5pt فوق خط أساس 12pt (0.54 من حجم الخط)، وأكبر انحراف شريف — glyph مسافة — 1.7pt (0.17). القطع عند 0.35.

**قواعد تقطيع اتغيرت نتيجة النص الحقيقي (بتعدّل ADR-002):**

- **الترويسة لازم تشغل سطرها لوحدها.** `المادة (21) من هذا القانون ...` بتبدأ سطراً ملفوفاً وهي **إحالة مش ترويسة**؛ من غير الشرط ده فتحت مادة 21 تانية وقطعت المادة 21 الحقيقية نصين. المقاس على الـ PDF: **56 سطر ترويسة خالص** مقابل **إحالة واحدة**.
- **إعادة بدء الترقيم بتفصل قانون الإصدار عن القانون المرافق.** الـ PDF ده بيرقّم مواد الإصدار `المادة (1)` مش `المادة الأولى`، فنمط الترتيب في ADR-002 مبيلقطهاش والكتابين بيتصادموا (تكرار 1..7). الإشارة هي **إعادة البدء**: أول ترويسة رقمها مش أكبر من اللي قبلها. اللي قبلها = إصدار. مستند مبيعيدش البدء بيفضل زي ما هو.

**النتيجة المقيسة:** 7 مواد إصدار + 49 مادة قانون = **الهيكل المنشور بالظبط** لقانون 151/2020، و`structure OK`. الباقي من التشوّه: **8 كلمات** فيها ألف مكررة من 35,071 حرف (مسجّلة في `SOURCE.txt`).

**البدائل المرفوضة:**
- *`arabic_reshaper` + `python-bidi`* — الاتنين بيحوّلوا **منطقي ← بصري**، وإحنا محتاجين العكس. مش موجود عكسهم كمكتبة.
- *عكس السلسلة النصية بعد `extract_text()`* — أبسط سطرين، وبيفسد كل لام-ألف وكل رقم. جُرّب فعلاً وأنتج `الإخلال` ← `اإلخالل`.
- *OCR للنسخة الرسمية بدل إصلاح الاستخراج* — الصح مبدئياً (النسخة الرسمية هي المرجع)، لكن الجريدة الرسمية ممسوحة ضوئياً و`tesseract` مش متثبت. **مؤجل، مش مرفوض** — والفحص لسه مفتوح في `SOURCE.txt`.

**شرط إعادة النظر:** لو دخل PDF من مصدر تاني وفشل فيه الاستخراج، تُقاس الأرقام (نسبة العربي، تباعد السطور، ارتفاع العلامات) على المستند الجديد قبل تعديل أي ثابت — الثوابت دي مقيسة على مستند واحد، مش قوانين طبيعة.


---

## ADR-014 — الأسئلة تحت الحد الأدنى للتداخل تُنقل لفئتها، ولا تُعدَّل صياغتها
**2026-09-08 · مقبول** · يفعّل شرط إعادة النظر في ADR-009

**السياق:** حارس ADR-009 له طرفان. الطرف الأعلى واضح: سؤال منسوخ من المادة يكسب استرجاعاً مجانياً. الطرف الأدنى ظهر عند أول تشغيل حقيقي — خمسة أسئلة (Q002 0.00 · Q003 0.14 · Q007 0.00 · Q010 0.12 · Q012 0.11) سقطت تحت الحد الأدنى لفئتها، فرفض الحارس ختمها `verified` وعطّل معيار M1/A3.

**الخياران:** إضافة مصطلح ملموس من موضوع المادة لرفع التداخل، أو نقل السؤال إلى `colloquial`.

**القرار: النقل.** المرساة الوحيدة المتاحة في كل حالة هي **لفظ المادة نفسه** («مسؤول حماية البيانات» في Q002، «التسويق الإلكتروني» في Q007). إضافتها ترفع الرقم بتحويل السؤال إلى صياغة القانون بدل صياغة من يسأل — وهو بالضبط ما وُضع ADR-009 ليمنعه. المقياس يُصلَح بتخريب ما يقيسه. `colloquial` هي الفئة الموجودة لاختبار متانة إعادة الصياغة عمداً، ونطاقها `[0.0, 0.35]` يسع الخمسة.

**القاعدة العامة:** تجاوز الحد **الأعلى** ⇒ يُعاد صياغة السؤال. النزول تحت الحد **الأدنى** ⇒ تُصحَّح الفئة، لا الصياغة. السبب: التجاوز الأعلى عيب في السؤال؛ النزول الأدنى تصنيف خاطئ — السؤال سليم وفي المكان الغلط.

**التسجيل لم يتأثر:** `recall_at_k` يقرأ `expected_articles` لا `category`. Q010 و Q012 احتفظا بمادتيهما، فنصف الدرجة على مادة واحدة كما هو. `category` تحكم اختيار النطاق وخانة التقرير فقط.

**النتيجة المقيسة — وهي مبرر القرار البعدي:**

| | direct | colloquial |
|---|---:|---:|
| قبل | 0.429 (7 أسئلة) | 0.333 (3) |
| بعد | **0.750** (4) | **0.125** (8) |

الإجمالي لم يتغير (0.333) — نفس الأسئلة ونفس المسترجِع. لكن الرقمين قبل النقل كانا متقاربين ومضلِّلين: أسئلة إعادة الصياغة كانت مبعثرة داخل `direct` تسحبه لأسفل وترفع `colloquial`. بعد التصحيح تظهر الفجوة الحقيقية **×6**، وهي المقياس الذي يُحكم به على النموذج الكثيف — لا الإجمالي.

**التكلفة، مقبولة ومسجّلة:** `direct` من 7 إلى 4 وفقدت تغطية `law-8` و `law-17`؛ `multi_article` من 5 إلى 3؛ Q002 و Q007 لهما توأمان عامّيان أصلاً (Q014، Q015) فدخل تكرار على `colloquial`. الفئات الآن أرق من أن يُبنى عليها استنتاج، والدفعة الثانية (val) تُكتب لتعويض `direct` و `multi_article` تحديداً.

**البدائل المرفوضة:**
- *خفض الحد الأدنى إلى الصفر لكل الفئات* — يلغي الحارس بدل أن يستجيب له. الحد الأدنى في `direct` موجود لسبب: `direct` هي الفئة التي تُثبت أن BM25 يعمل حين توجد مرساة، ولو خلت من المراسي فقدت معناها.
- *ختم الأسئلة `verified` مع تجاوز يدوي* — يفتح باباً لتجاوز كل حارس لاحق.

**شرط إعادة النظر:** لو نزلت `direct` أو `multi_article` تحت 5 أسئلة بعد الدفعة الثانية، فالمشكلة في تصميم الفئات لا في الأسئلة، ويُعاد النظر في فصل محورَي «الصياغة» و«عدد المواد» إلى حقلين مستقلين بدل فئة واحدة تخلطهما.

---

## ADR-015 — نموذج الـ embeddings، والحقل الذي يُدخَل إليه
**2026-09-08 · مقبول** · يحسم القرار المؤجَّل في PRD ومنذ أسبوع 1

**السياق:** القرار كان مؤجلاً عمداً لحين وجود رقم BM25 يُقارن به. الرقم موجود (0.333).

**القرار:** `intfloat/multilingual-e5-base` — 278M معامل، 768 بُعداً، ‏530 ميجابايت، يعمل على CPU.

**لماذا هو:** مُدرَّب على **الاسترجاع** لا على التشابه العام، وهذا فرق جوهري: نموذج التشابه يقارن نصين متكافئين، والاسترجاع سؤال قصير مقابل فقرة طويلة (غير متماثل). موثَّق وواسع الاستخدام، فالرقم قابل لإعادة الإنتاج من غيرنا.

**البدائل المرفوضة:**
- *`paraphrase-multilingual-mpnet-base-v2`* — مُدرَّب على التشابه المتماثل، وهو الاستخدام الخطأ هنا.
- *`multilingual-e5-small`* — أسرع وأصغر، لكن المتن 56 مادة والفارق في زمن التنفيذ لا يُذكر. الجودة أولى.
- *نموذج عربي متخصص* (GATE-AraBert وغيره) — واعد، لكن تقييمه مشروع قائم بذاته. **مؤجل لا مرفوض**؛ يُقارن بـ e5 كخط أساس بعد تثبيت مجموعة val.

**الجزء الأهم في هذا القرار ليس النموذج بل الحقل الذي يُدخَل إليه:**

الفهرس المعجمي يُبنى على `search_text` — الحقل المطبَّع بعدوانية (تقشير السوابق، توحيد الهمزة، حذف التشكيل). هذا صحيح لـ BM25 وهو سبب وجود ADR-004. وهو **خطأ للنموذج**: المحوّل مُدرَّب على عربية طبيعية، وما يراه BM25 تطبيعاً يراه النموذج تلفاً.

لذلك: **الكثيف يُضمِّن `text` الحرفي، والسؤال يصل إليه كما كُتب — بلا `search_normalize`.** والسابقتان `query: ` و `passage: ` إلزاميتان لأن E5 غير متماثل ودُرِّب عليهما نصاً.

**لماذا هذا مكتوب هنا:** الأخطاء الثلاثة (الحقل الخطأ، إسقاط السابقة، تطبيع السؤال) **لا تُصدر أي خطأ**. تُنتج رقماً كثيفاً منخفضاً يبدو معقولاً، فيُقرأ كـ«النماذج الكثيفة لا تنفع مع العربية» — استنتاج خاطئ عن اللغة سببه سطر توصيل. `tests/test_dense.py` يثبّت الثلاثة بمشفِّر بديل، بلا تنزيل ولا شبكة.

**شرط إعادة النظر:** أي نموذج بديل يُقاس على **نفس** الأسئلة ونفس k، ويُسجَّل في `EVAL.md` بجانب e5 لا مكانه.

---

## ADR-016 — دمج النتائج بـ RRF لا بمجموع موزون
**2026-09-08 · مقبول**

**السياق:** التكوين الهجين يحتاج دمج ترتيبين: BM25 ودرجات جيب التمام.

**القرار:** Reciprocal Rank Fusion — `score(d) = Σ 1/(60 + rank)`.

**السبب:** درجات BM25 غير محدودة وتعتمد على المتن؛ جيب التمام في `[-1, 1]` ويتكدّس قرب القمة مع E5. الدمج بالقيمة يستلزم وزناً يُضبَط على هذا المتن — و**ضبط وزن على 15 سؤالاً طريقة للإفراط في المواءمة لا للقياس**. الـ RRF يقرأ الرتب فقط، فثابته الوحيد لا يعتمد على أي مقياس درجات. الاختبار `test_rrf_ignores_score_scale` يثبّت هذا: ضرب درجات مسترجِع في 1000 لا يغيّر شيئاً.

**شرط إعادة النظر:** الوزن غير المتساوي (ترجيح الكثيف) خيار مشروع — لكن يُختار على **val** لا على dev. اختياره على dev بعد رؤية النتيجة هو ضبط على مجموعة الاختبار.

---

## ADR-017 — إعادة الترتيب بـ cross-encoder فوق مرشّحي الهجين
**2026-09-08 · مقبول**

**القرار:** `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1` على أعلى **20** مرشحاً من الهجين.

**التفصيلة التي يسهل الخطأ فيها ويستحيل رؤيتها بعدها:** يُمرَّر للنموذج **النص الكامل للمادة**، لا `Hit.snippet`. الـ snippet حقل عرض مقطوع عند 280 حرفاً للواجهة؛ التقييم عليه يبتر كل مادة بعد فقرتها الأولى — والحكم العملي في القانون كثيراً ما يقع في الثانية. (وقعت في هذا الخطأ أثناء الكتابة؛ الاختبار `test_reranker_reads_the_full_article_not_the_snippet` يمنع رجوعه.)

**السقف يُنشر مع النتيجة:** المُعيد لا يرتّب إلا ما أُعطي له. إن غابت المادة عن المرشحين لا يستعيدها شيء. لذلك يُطبع `recall@20` لمرحلة المرشحين بجانب النتيجة — رقم إعادة ترتيب بلا سقفه غير قابل للقراءة.

---

## ADR-018 — نتيجة فحص الأمانة: المتن المُستوعَب ليس القانون كما نُشر
**2026-09-08 · مقبول** · يُبطل ادعاءً في ADR-013، ويقيّد كل رقم في `EVAL.md`

**السياق:** الفحص الإلزامي (مواد عشوائية مقابل المسح الرسمي) كان مفتوحاً `__/10` منذ أول تشغيل. نُفِّذ اليوم على **11 مادة**.

**النتيجة: رسوب. 9 من 11 مادة تختلف عن الجريدة الرسمية.** المطابقتان هما 11 و12 (فروق صيغة همزة فقط).

### الاختلافات ليست ضجيج استخراج

أخطرها في المادة 7، وهي المادة التي تعتمد عليها مجموعة التقييم:

| | الجريدة الرسمية | المُستوعَب |
|---|---|---|
| م7 | «اثنتين وسبعين ساعة» | «اثنين وسبعين ساعة» |
| م7 | «بموافاة المركز» | «بموافاته» |
| م1 | «بالكلمات والعبارات» | «بالمصطلحات» |
| م6 | «أى من الحالات» | «إحدى الحالات» |
| م6 | «عقد لصالح» | «عقد الصالح» |
| م10 | «أن يكون بناءً» | «أن تكون بناء» |
| م17 | «الإشارة إلى أن» | «الإشارة بأن» |
| م18 | «إلكترونى» | «إليكتروني» (3 مرات) |
| م3 | «اللائحة التنفيذية **لهذا القانون**» | حُذفت |
| م5 | «هذا القانون» | «هذا القنون» |

**الدليل على أن المصدر هو السبب لا كودنا:** كلمة «اثنتين» ترد **صفر مرة** في النص المستخرج من نسخة `privacylaws.com`، و«بالمصطلحات» ترد مرة و«بالكلمات» صفر. كلمات مختلفة بالكامل لا تنتج عن خطأ في المسافات. النسخة المعاد نشرها **نص آخر**، لا نسخة من النص.

### الأثر على مجموعة التقييم — يحتاج فعلاً لاحقاً

`Q001` و`Q013` تحملان `expected_keywords: ["اثنين وسبعين", "ساعة"]`. هذه العبارة **غير موجودة في القانون كما نُشر**. رقم المادة (law-7) **صحيح في المصدرين**؛ الخطأ في الكلمة المفتاحية وحدها، وهي مربوطة بالنسخة المعاد نشرها.

`verify-refs` يمرّ اليوم لأنه يفحص مقابل المتن المُستوعَب. **عند إعادة الاستيعاب من OCR للجريدة سيسقط السؤالان — وهذا هو السلوك الصحيح**، وتصير الكلمة «اثنتين وسبعين».

### الأثر على كل رقم

Run 1 و Run 2 **أرقام عن هذه النسخة المعاد نشرها**. هي:
- **صالحة** كمقارنة استرجاع — التكوينات الأربعة رأت المتن نفسه بالضبط، فالفروق بينها حقيقية.
- **غير صالحة** كقول عن قانون 151/2020 المصري.

التمييز جوهري: 0.767 مقابل 0.333 يظل نتيجة عن `e5-base` مقابل BM25. لكن «النظام يجيب عن قانون حماية البيانات المصري» **لا يصح قولها** بهذا المتن.

### ADR-013 كان يدّعي «0 كلمات مكسورة متبقية» — الادعاء خاطئ

**20 حالة في 13 مادة**: «عشر ين» (عشرين)، «تو افر» (توافر)، «مض ي» (مضي)، «نشر ها»، «المع الجة»، وغيرها. كُشفت بمعيار قابل لإعادة التشغيل: الشكل الملتحم يرد في المتن نفسه كتوكن مستقل.

**المنشأ غير محسوم ولم يُنشر تقسيم:** منطق `pdfplumber` المستقل يعيد إنتاج بعض هذه الكسور (فهي في المستند) دون بعضها (فهي من إعادة بنائنا). الفحصان تعارضا، وعكس الترتيب البصري ساذجاً غير موثوق مع روابط العربية. **الثابت:** الكسور موجودة، وتخفض الاسترجاع المعجمي لأن BM25 يفهرس «عشر» و«ين» كتوكنين.

### القرار

1. **لا يُنشر أي رقم كقول عن القانون** قبل استيعاب متن أمين. كل جدول يحمل قيد المصدر.
2. الجريدة الرسمية **نُزِّلت وهي الآن على القرص** بمصمة معروفة. استيعابها يحتاج OCR.
3. **OCR جُرِّب ولم يُعتمد** — التفاصيل في ADR-019.
4. `Q001`/`Q013` **تُترك كما هي اليوم** لأن المتن الحالي هو النسخة المعاد نشرها؛ تغييرها الآن يكسر `verify-refs` بلا سبب. الفعل مسجَّل في `SOURCE.txt` ومشروط بإعادة الاستيعاب.

**شرط إعادة النظر:** لا يوجد. هذه نتيجة قياس، لا اختيار تصميم.

---

## ADR-019 — EasyOCR جُرِّب على الجريدة ولم يُعتمد كمتن
**2026-09-08 · مقبول**

**السياق:** `ocrmypdf`/`tesseract` غير مثبتين. أوزان PaddleOCR العربية موجودة في كاش الجهاز، **لكن `paddlepaddle` بلا wheel لبايثون 3.14** فالمسار مغلق. `easyocr` يعمل على torch المثبت أصلاً.

**ما قيس فعلاً:** المسح نظيف (طباعة رقمية، 1339×1916، لا تصوير رديء). EasyOCR **83–99 ثانية للصفحة** على CPU، أي ~45 دقيقة للمستند.

**الجودة — أكّد وأخطأ في آن:**

✅ صحّح فرضيتين مستقلتين: قرأ «ببياناته» (المُستوعَب «بياناته») و«عشرين ألف جنيه» (المُستوعَب «عشر ين»). أي أن OCR **شهد لصالح الجريدة ضد النسخة المعاد نشرها** بشكل مستقل عن قراءتي.

❌ أخطاء منهجية: يُسقط الميم في نمط «الـم» (`المحو`←`الحو`، `المركز`←`الركز`، `المعالج`←`العالج`)، **ويخرّب الأرقام** (`١٥`←`٥`، `٢٠٢٠`←`٢٠٢`، `(٥)`←`٥١(`)، ويُسقط الترقيم.

**القرار: يُستخدم للتحديد والإسناد، لا كنص مرجعي.**

**السبب:** الأرقام هي بالضبط ما يحتاجه متن قانوني — أرقام المواد، المهل، مبالغ الغرامات. أداة تخطئ في `١٥` تنتج متناً يبدو سليماً ويعطي مهلة خاطئة، وهو أسوأ من عدم وجود متن. أما دورها الحالي — تأكيد قراءة بصرية لعبارة بعينها — فسليم لأن الخطأ يظهر عند التعارض.

**البدائل:** `surya-ocr` (له wheel لـ3.14، أقوى على العربية، نماذج أكبر) **غير مُجرَّب — المرشح التالي**. أو `ocrmypdf` بعد تثبيت tesseract خارج بايثون. أو شراء/طلب نسخة رقمية رسمية.

**شرط إعادة النظر:** أي محرك يُقاس أولاً على **دقة الأرقام** في صفحة معروفة قبل تشغيله على المستند كله.

---

## ADR-020 — بوابة دقة الأرقام تصير كوداً، و PaddleOCR يسقط عندها
**2026-09-09 · مقبول**

**السياق:** ADR-019 كتب الشرط — «أي محرك يُقاس أولاً على **دقة الأرقام** في صفحة معروفة قبل تشغيله على المستند كله» — ثم تركه جملةً في وثيقة. وهذا بالضبط شكل ادعاء ADR-013 «صفر كلمات مكسورة متبقية» قبل أن يتضح خطؤه بعد أن صار قابلاً للتشغيل. ادعاء لا يُشغَّل ليس معياراً، بل نيّة.

**القرار: البوابة كود** — `src/legalrag/ocr_gate.py` + `python tasks.py ocr-gate <json>`، وحقيقة أرضية في `evals/ocr/digit_ground_truth.json`.

**لماذا الأرقام لا الحروف.** المقياس المعتاد لـ OCR هو دقة الحروف، وبه تسجّل الجريدة نتيجة جيدة: طباعة رقمية نظيفة والنثر يخرج مقبولاً. النثر ليس ما ينكسر. حرف خاطئ داخل جملة يراه أي قارئ؛ **رقم** خاطئ لا يراه أحد، لأن «المادة (١٢)» و«المادة (١٢١)» سليمتان شكلاً تماماً. أرقام المواد هي المفتاح الأساسي لهذا المتن: تقطّعه، وهي ground truth كل سؤال، وكل إحالة تُحلّ من خلالها. رقم واحد مشوَّه يعيد توصيل الثلاثة بصمت ولا يُخرج خطأً في أي مكان.

**لماذا مقارنة مجموعات لا مسافة تحرير.** محرّك يقرأ `٢٠٢٠` كـ`٢ ٠` احتفظ بثلاثة أحرف من أربعة وفقد **العدد**؛ ومحرّك يقرأ `٣٦،` كـ`٣٦١` اخترع مادةً غير موجودة. مقارنة **الرموز الكاملة** تحسب الاثنين إخفاقاً، وهي القراءة الأمينة. و«ناقص» و«مخترَع» يُبلَّغان منفصلين لأنهما يختلفان في الأثر: الناقص يفشل بصوت عالٍ عند التقطيع، والمخترَع لا يفشل أبداً.

**الحقيقة الأرضية مقروءة بالعين من صور الصفحات، لا مُشتقّة من محرّك** — محرّك لا يصلح مسطرةً لنفسه. صفحتان (7 و28)، 31 رمزاً، مختارتان لتغطية كل صورة يظهر بها رقم: ترويسة صفحة، ترويسة مادة، إحالة داخل النثر، وقائمة مفصولة بفواصل.

**ما قيس:**

ثلاث صفحات (7 · 10 · 28)، 42 رمزاً:

| المحرّك | التكوين | دقة الأرقام | الحالات الحرجة | ث/صفحة |
|---|---|---:|:--:|---:|
| easyocr 1.7.2 | افتراضي | **71.4%** | 0/3 | 83–99 |
| PaddleOCR 3.7.0 | `server_det` + `arabic_PP-OCRv5_mobile_rec` | **21.4%** | 0/3 | 590–669 |
| PaddleOCR 3.7.0 | `mobile_det` + نفس نموذج التعرّف | **16.7%** | 0/3 | 75–119 |
| **surya-ocr 0.17.1** | torch/CPU | **92.9%** | **3/3 ✓** | 160–198 |

**النمطان مختلفان، وهذا هو الجزء المفيد.** easyocr **يشوّه** (`(١٢)`←`١٢١`، الفاصلة العربية تُقرأ رقماً) — صامت ويخترع مواد. Paddle **يحذف** (`مادة (٤)`←`مادة`) — ونثره العربي أنظف من easyocr بوضوح، لكن بلا أرقام لا يوجد ما يُقطَّع. الحذف أأمن لأنه يفشل بصوت عالٍ، وغير قابل للاستخدام بالقدر نفسه.

**فرضية اختُبرت وسقطت:** الالتحام في `العددمكرره` مع غياب الرقم بينهما يشبه قصّاً في **الكشف**. تبديل نموذج الكشف لم يحسّن شيئاً (22.6% ← 19.4%). ثم أُزيلت مرحلة الكشف كلياً: قصاصات أسطر مقصوصة يدوياً غُذِّيت نموذج التعرّف مباشرة، فقرأ `مـادة ( ٤٧ ) :` كـ`مادة٤`، وقرأ سطر الترويسة الكامل — الذي يحمل **أربعة** أرقام — بلا أي رقم، مع بقاء الكلمات العربية سليمة. **العجز في نموذج التعرّف العربي نفسه.**

**تصحيح لـ ADR-019 في نقطة واقعية:** قال إن مسار paddle مغلق لأن `paddlepaddle` بلا wheel لبايثون 3.14. صحيح عن بيئة المشروع، **خطأ كاستنتاج** — الحزمة والأوزان العربية كانتا مثبَّتتين بالفعل في بيئة Python 3.12 منفصلة على الجهاز نفسه. المسار كان مفتوحاً؛ لكنه لا يؤدي إلى شيء. وتصحيح ثانٍ: «`surya-ocr` له wheel لـ3.14» مضلِّل — الـ wheel `py3-none-any`، لكن قيده `pillow<11` غير قابل للإشباع على 3.14 (أول wheel لـcp314 هو Pillow 11.3.0)، فتثبيته كان سيُنزِل Pillow من 12.3.0 إلى 10.4.0 من تحت easyocr و sentence-transformers. لذلك: **بيئة conda معزولة، لا تثبيت في بيئة المشروع.**

**surya يرسب في العتبة وينجح في كل ما تقيسه العتبة من أجله.** 92.9%، وصفحتان من ثلاث **100%** — منهما ص10 التي تحمل المادة 7، مدار فحص الأمانة كله. الأخطاء الثلاثة كلها في ص28: رقم صفحة في الهامش (ليس جزءاً من المتن) وذيل قائمة إحالات طويلة. **كل ترويسات المواد صحيحة، وعليها يقوم التقطيع.** وحدث ما يستحق التسجيل: surya — بمعمارية مختلفة كلياً عن easyocr — أعاد إنتاج **كل** نتيجة في فحص الأمانة بصفر تداخل («اثنتين» مرتين، «بموافاة»، «لصالح»، «الإلكترونية»، «بالكلمات والعبارات»)، فصار شاهداً ثالثاً مستقلاً بعد القراءة البصرية و easyocr.

**قيدان على surya يجب ألا يضيعا:** (1) ليس نسخاً حرفياً — يطبّع الألف المقصورة إلى ياء (`فى`←`في`)، وهو فرق يطويه `search_normalize` فلا يراه الاسترجاع، لكن `evaluation_normalize` يراه. (2) يُخرج وسوم HTML (`<b>مادة</b>`)، فلا يصلح مدخلاً لـ`ingest` مباشرة — `src/legalrag/ocr_text.py` يزيلها ويزيل أثاث الصفحات ويرتّب الصفحات عددياً.

**التكبير لا يساعد.** المسح 1339×1916 عند 72 ppi وهو كل المعلومة الموجودة، لكن النماذج تُصغّر داخلياً فما ينجو يعتمد على المُدخل. جُرّب ×2 LANCZOS كـ`highres_images`: النتيجة على نفس الصفحتين **لم تتغير**، والأخطاء **انتقلت** حتى صارت `مادة (٤٨)` تُقرأ `١٨` — أي أن ترويسة مادة فسدت بعد أن كانت صحيحة. الحد هو المسح، و1× هو التكوين الأفضل.

**البوابة شاشة فحص لا شهادة.** النجاح يعني «يستحق تشغيلاً كاملاً ثم تحققاً مقابل المستند»؛ 31 رمزاً على صفحتين لا تُثبت صحة 29 صفحة. **الرسوب هو الاتجاه الحاسم.** العتبة 99% — ليست شريط جودة بل النقطة التي دونها لا يمكن تقطيع المتن أصلاً، لأن ترويسات المواد **هي** أرقام.

**البدائل الباقية:** إصدار أقدم من surya يعمل على torch (0.22 تخلّى عن مسار torch: backends متاحة `vllm` بـDocker+GPU، و`llamacpp` بـbinary وأوزان GGUF، و`openai_client`). أو tesseract بـ`ara` — يحتاج binary ويندوز خارج pip/conda. أو نسخة رقمية رسمية من الجريدة.

**شرط إعادة النظر:** يسقط هذا القرار متى نجح محرّك في البوابة؛ وقتها يُشغَّل على 29 صفحة ويُعاد التحقق مقابل المستند قبل أن يصير متن السجل.

---

## ADR-021 — متن الجريدة قيس ولم يُعتمد؛ المتن الحالي يبقى بقيده الموثَّق
**2026-09-10 · مقبول (قرار محمد)**

**السياق:** بعد ADR-020، شُغِّل `surya-ocr 0.17.1` على الـ29 صفحة كاملة، وحُوِّل بـ`ocr-to-raw`، واستُوعِب في **نسخة معزولة** (لم يُمَسّ `data/processed/articles.jsonl` في أي لحظة).

**ما قيس — والفحص هنا أقوى من بوابة الـ42 رمزاً:** المتن انقسم إلى **56 مادة: 7 إصدار + 49 قانون**، وهي البنية المنشورة بالضبط. المقارنة مادة بمادة مع المتن الحالي:

| المقياس | القيمة |
|---|---|
| متوسط التشابه — `search_normalize` (ما يراه الاسترجاع) | **0.988** |
| متوسط التشابه — `evaluation_normalize` (حرفي) | **0.973** |
| مطابقة (≥0.99) | 38 / 56 |
| ≥0.90 | **55 / 56** |
| أرقام ترويسات قرأها OCR خطأً | **3** (38←28 · 42←21 · 44←144) |

**المادة الوحيدة تحت 0.90 هي `issuance-7`، والفرق لصالح الجريدة:** فيها بيان التصديق الرئاسي («صدر برئاسة الجمهورية فى ٢٢ ذى القعدة سنة ١٤٤١هـ… عبد الفتاح السيسى») **محذوف من النسخة المعاد نشرها**. الجريدة أكمل لا أنقص. وكسر ثانٍ حُسم منشؤه: المتن الحالي فيه `مض ي` والجريدة فيها `مضى` سليمة (بعد «عشرين» في ADR-018).

**الأخطاء الثلاثة كلها قابلة للتصحيح آلياً من موضعها** — `28` بين `37` و`39` فجوة وتكرار في آن، والتحقق البنيوي يصطادها. أي أن نصاً مصحَّحاً كان متاحاً.

**القرار (محمد): لا يُستبدل المتن.** الجريدة تُحفظ كدليل مقيس في `EVAL.md` و`SOURCE.txt`، والمتن الحالي يبقى بقيده الموثَّق، ويُنتقل إلى M2.

**السبب المقبول:** التصحيح من الموضع **تدخّل بشري في نص السجل**، وقيمته هنا لا تبرّره: أرقام الاسترجاع كلها **صالحة كمقارنة** أصلاً (التكوينات الأربعة رأت المتن نفسه بالضبط)، والفرق الذي كان سيُشترى بالتدخل هو تحويلها إلى **قول عن القانون** — وهي ترقية في الادعاء لا في القياس. وبالتشابه 0.988 على نظرة الاسترجاع، **الأرقام نفسها لن تتحرك تقريباً**.

**ما لا يتغير نتيجة هذا القرار — ويجب ألا يضيع:**
- `Q001`/`Q013` تبقيان على «اثنين وسبعين»، والتصحيح إلى «اثنتين وسبعين» **مؤجَّل بشرط** كما هو.
- كل رقم في `EVAL.md` يبقى **صالحاً كمقارنة استرجاع وغير صالح كقول عن قانون 151/2020**. القيد لم يُرفع.
- فحص الأمانة صار له **ثلاثة شهود مستقلين**: القراءة البصرية للصور، easyocr، surya — بصفر تداخل في كل عبارة.

**شرط إعادة النظر:** يسقط هذا القرار متى (أ) توفّرت نسخة رقمية رسمية بلا OCR، أو (ب) نجح محرّك في بوابة `ocr-gate` بلا تدخّل، أو (ج) صار المشروع يقتبس نص المواد للمستخدم — عندها الفرق بين «قريب» و«حرفي» يتوقف عن كونه أكاديمياً.

---

## ADR-022 — التوليد المحلي: النموذج والـdtype وشكل الفحص
**2026-09-10 · مقبول**

**السياق:** M2 يحتاج مولِّداً محلياً. المتاح فعلاً على هذه البيئة: `llama-cpp-python` **بلا wheel لبايثون 3.14 إطلاقاً**، و Ollama غير مثبّت، و`transformers` + torch موجودان بالفعل. فالمسار واحد: transformers على CPU.

**القرار الأول — الـdtype قبل الحجم.** جُرِّب `Qwen2.5-3B-Instruct` بـ`dtype="auto"`. الـcheckpoint بـ bfloat16 وهذا الـCPU بلا دعم أصلي له، فكل matmul يُحاكى: **إجابة قصيرة واحدة استغرقت ست ساعات** (تحميل 6,465 ث، توليد 15,499 ث — طوابع زمنية لا تقديرات). نفس الكود بـ`float32`: تحميل **87.7 ث**، توليد **0.85 tok/s**. أسرع ~1400×.

**الدرس الذي يستحق التسجيل أكثر من الرقم:** `device_map="cpu"` كان خطأً أيضاً — و**أخرج استثناءً** فاكتُشف في ثوانٍ. `dtype="auto"` **لم يُخرج شيئاً**؛ اشتغل وحسب، أبطأ بأربع مراتب عشرية، وشكله من الخارج «النظام معلّق» لا «إعداد خاطئ». الأعطال الصامتة أغلى من الصاخبة دائماً، وهذا المشروع قائم على اصطيادها بالقياس.

**القرار: `Qwen/Qwen2.5-1.5B-Instruct` · float32 · greedy · max_new_tokens=128.** الحجم **نتيجة تابعة** للـdtype لا اختيار مستقل: 3B في float32 = 12.4 GB مقابل 16 GB من الذاكرة، فيقع في swap. greedy لأن B1 قول عن النظام لا عن عيّنة واحدة منه.

**القرار الثاني — الفحص لا يحوي نموذجاً.** `cite.py` يعمل على النص مقابل المتن ومقابل ما سُلِّم للمولِّد، فيصلح مهما كان المولِّد. وثلاثة أعطال تُعدّ منفصلة لأنها تقول أشياء مختلفة: `fabricated` (اخترع تشريعاً)، `ungrounded` (استرجعها من ذاكرته لا من السياق — **وتجتاز أي فحص يسأل «هل الرقم موجود؟»**)، `uncited` (ادّعاء بلا سند).

**خطآن في الفحص نفسه، صُحِّحا بالتشغيل:**
- **B1 كان يمرّر إجابات لا تستشهد بشيء.** نص المعيار «صفر إجابة **بلا استشهاد**»، فالإجابة بلا سند خرق. الحكم كُتب `not fabricated and not ungrounded` وهو ما يجتازه نموذج لا يستشهد أصلاً — وطبعت الأداة `PASS` على 12 إجابة بلا سند. مقفول بستة اختبارات.
- **الاستشهاد داخل نص منسوخ ليس استشهاداً.** القوانين تحيل على نفسها؛ حين ينسخ النموذج المادة، تُقرأ الإحالة على أنها إسناد. صحّح «استشهد بالمادة المتوقعة» من 21.4% إلى **14.3%**.

**حد مسجَّل لا مُغطّى:** الفحص يتأكد أن الاستشهاد يُحَل وأنه كان ضمن المسترجَع — **لا أنه يسند الجملة**. `Q002` استشهدت بالمادة 7 (الإبلاغ عن الاختراق) لدعم دعوى عن تعيين مسؤول خصوصية: مسنَد ميكانيكياً، خاطئ موضوعياً. الحكم على الاتساق الدلالي يحتاج بشراً أو نموذجاً حكماً، وكلاهما خارج M2 كما هو معرَّف.

**شرط إعادة النظر:** يُعاد النظر في النموذج متى توفّر runtime أسرع (wheel لـ`llama-cpp-python`، أو Ollama، أو تسريع GPU) — عندها يصير 3B أو أكبر قابلاً للقياس، ويصبح السؤال «هل رسوب B1 خاصية النموذج أم خاصية الحجم؟» قابلاً للإجابة بدل التخمين.

---

## ADR-023 — طبقة demo محلية داخل P1: رفع مستند والسؤال عليه باستشهاد متحقَّق منه
**2026-09-14 · مقبول**

**السياق:** خطة الـportfolio تشترط قبل أي خطوة جديدة «repo منشور + أرقام + فيديو 3 دقايق» (`AI-Portfolio-Plan.md:124`)، والفيديو يحتاج شيئاً يعمل. وفي الخطة نفسها رفض صريح لمشروع «Chat with your PDF» العام (`:137`)، وPRD §4.3 كان يستبعد «واجهة مستخدم أو تطبيق ويب» و«أكتر من قانون واحد» و«نشر كخدمة».

**القرار:** طبقة demo **داخل P1**، لا مشروع جديد: مستخدم واحد، على الجهاز، يرفع PDF/TXT عربياً ويسأل عليه. ثلاثة شروط تفصلها عن «Chat with your PDF»، وسقوط أيٍّ منها يُسقط القرار:
1. **كل جملة تُعرض لها مصدر يتحقق منه كود بلا نموذج، قبل العرض.** امتداد مباشر لـ`cite.py` ولقاعدة M2: «إجابة بلا سند = مرفوضة قبل ما توصل للمستخدم».
2. **الدقة منشورة لا مُدّعاة.** مجموعة تقييم للتطبيق تُكتب قبل كوده (`evals/app/`)، وتاريخ git هو الدليل.
3. **محلي بالكامل.** التوليد على الجهاز بلا API مدفوع، والقصة «يشتغل on-prem».

**ما تغيّر في PRD:** الواجهة المحلية لمستخدم واحد دخلت النطاق (§4.4). «قانون واحد» بقي قيداً على **متن الـbenchmark**، لا على ما يرفعه المستخدم. الاستضافة والادّعاء القانوني **بقيا خارج النطاق**.

**ما لم يتغيّر:**
- المتن الحالي نسخة معاد نشرها بلا رخصة موثّقة (`data/raw/SOURCE.txt`) ولم تجتز فحص الأمانة (ADR-018)، فهو **لا يدخل git ولا صورة Docker**. من يشغّل الـdemo يرفع ملفه بنفسه.
- لا طبقة providers؛ مكانها P3. مفتاح بسيط `hf:` / `ollama:` فقط.
- تغيير واحد لكل Run مقاس، وقاعدة الاختيار تُكتب قبل التشغيل.

**تصحيح لـADR-022:** سياقه قال «Ollama غير مثبّت». هو مثبّت (`D:\Ollama`، 0.20.3)، لكن خارج PATH، فلم يظهر لـ`where ollama`. شرط إعادة النظر في ADR-022 تحقّق إذن، ويُقاس في Run 4 (ADR-024).

**التكلفة:** ~17 ساعة من ميزانية P1 (40، سقف 52). الإنفاق قبل اليوم غير مسجّل، فيُفتح سجل ساعات في `docs/internal/PROJECT-STATE.md`. الـ40 سؤالاً الباقية وB3–B5 تتأخر بعد الطبقة.

**شرط إعادة النظر:** إن تجاوز العمل التقدير بأكثر من 25%، يُطبَّق خط القطع (Docker، ملف PDF الاصطناعي، Run 4a) قبل أي تمديد. وإن صار الهدف استضافة عامة، يسقط القرار كله، لأن الأمان ورخصة المتن والتكلفة تصير أسئلة مختلفة.

---

## ADR-024 — مولِّد التطبيق: `gemma3:4b` على Ollama، بقاعدة سُجِّلت قبل الأرقام
**2026-09-14 · مقبول · يراجع ADR-022**

**السياق:** ADR-022 اختار `Qwen2.5-1.5B` على transformers/CPU لأن «Ollama غير مثبّت». ADR-023 أثبت أنه مثبّت، فتحقّق شرط إعادة النظر. والتطبيق يحتاج مولِّداً محلياً يجيب في حدود 30 ثانية، وخط الأساس يأخذ 85.

**الإجراء:** Run 4 غيّر النموذج وحده. القاعدة ثُبِّتت في `d64b1ff` قبل أول إجابة: يُستبعد من يقل B2 عنده عن 80%، أو يزيد امتناعه الكاذب عن 20%، أو يتجاوز 30 ثانية؛ ثم يُختار الأعلى في «مسنَدة بالكامل». المرشحون: خط الأساس، `gemma3:4b`، `qwen2.5:7b-instruct`. أما `qwen3:4b` فاستُبعد قبل التشغيل بتجربة، لا بتخمين (EVAL.md، Run 4).

**القرار: `ollama:gemma3:4b`**، وهو الوحيد الذي اجتاز شروط الاستبعاد:
- مسنَدة بالكامل 11/14، وصيغة `[مادة N]` 14/14، والمادة المتوقعة 10/14.
- 10.9 ثانية للإجابة.

استُبعد `qwen2.5:7b-instruct` بامتناع كاذب 26.7%، وخط الأساس بـ85.4 ثانية.

**درسان يستحقان التسجيل:**
- **`think:false` لا يعني أن النموذج لن يفكّر.** على هذا البناء نقل التفكير إلى نص الإجابة، بلا أي خطأ. هو عطل صامت من عائلة `dtype="auto"` في ADR-022، واكتُشف بسؤال تجريبي واحد قبل Run كامل. كل مرشّح جديد يمر بسؤال تجريبي أولاً.
- **الأكبر خسر.** ‏7B لا يسع ذاكرة GPU بـ4 GB (نصفه فيها)، فصار أبطأ مرتين، وامتنع عن أسئلة لها إجابة.

**ما لم يُحسم:**
- **سبب تفوّق gemma** (العائلة؟ الحجم؟ الـruntime؟) غير معروف. أُلغي Run 4a بقرار محمد، فالمقارنة لا تعزل المتغيرات.
- **B1 ما زال رسوباً:** ثلاث إجابات سندها الوحيد نص منسوخ. هذا يُعالَج بشكل الإجابة والـgate (Run 5)، لا بتبديل النموذج.

**التخزين:**
- `gemma3:4b` موجود في مجلد النماذج الذي يشير إليه `D:\Ollama\start-ollama.bat`.
- **المقاس هو الـdigest، لا اسم الـtag:** `a2af6cc3eb7fa8be8504abaf9b04e88f17a119ec3f04a3addf55f92841195f5a` (Q4_K_M، 4.3B). وهو sha256 لملف الـmanifest، ويطابق ما يعيده `/api/tags`. أي `ollama pull` لاحق قد يبدّل الأوزان تحت الاسم نفسه؛ فإن تغيّر الـdigest، لم تعد أرقام Run 4 قولاً عن النموذج الموجود.
- أوزان خط الأساس نُقلت بقرار محمد إلى `models/Qwen/Qwen2.5-1.5B-Instruct/` داخل المشروع (المراجعة `989aa7980e4cf806f80c7fef2b1adb7bc71aa306`)، وتُقرأ من هناك قبل الـHF cache. المسار يتبع الـhub id حرفياً (`<org>/<name>`)، فلا يحمّل `hf:someorg/Qwen2.5-1.5B-Instruct` أوزان Qwen بصمت — وهو خطأ وجدته مراجعة الكود في الشكل الأول `models/<name>`.
- `models/` خارج git.

**شرط إعادة النظر:** أي مولِّد جديد يدخل بنفس الإجراء: تسجيل مسبق، ثم سؤال تجريبي، ثم تغيير واحد، على نفس الأسئلة. وإن تغيّر الـprompt أو السقف (Run 5)، يُعاد قياس المرشحين قبل أي تبديل.

---

## ADR-025 — شكل إجابة التطبيق: خطوة امتناع، ثم claims بـJSON، ثم gate بلا نموذج
**2026-09-14 · مقبول**

**السياق:** ADR-023 يشترط أن تكون لكل جملة تُعرض مصدرٌ يتحقق منه كودٌ قبل العرض. قيس شكلان قبل هذا القرار، ولم يصلح أيٌّ منهما وحده:
- **Run 4 (نص حر بـ`[مادة N]`):** يمتنع جيداً (5/5)، لكن الاستشهاد نص داخل الجملة يُقرأ بالـregex، ولا يصلح لمستندات لا مواد فيها.
- **Run 5 (JSON بمصادر مرقّمة + gate):** رفع عدد الأسئلة التي ذُكرت فيها المادة المتوقعة إلى 12/15، وأسقط الامتناع إلى 2/5.

**القرار:** لكل سؤال، ثلاث خطوات بالترتيب:
1. استدعاء امتناع بـschema `{"answers": bool}` وسقف 32 token. إن كان الجواب «لا»، يمتنع النظام.
2. إن كان «نعم»: claims بـJSON `{abstain, claims[{text, sources}]}` وسقف 384 token.
3. `cite.gate`، بلا نموذج، يُسقط أيَّ claim:
   - بلا مصدر؛
   - أو بمصدر خارج 1..k؛
   - أو تذكر مادةً ليست من مصادرها ولا مذكورةً في نصوصها.
   
   إن لم يبقَ شيء، يمتنع النظام.

التطبيق يعرض الـclaims الباقية فقط، ومع كلٍّ منها مصادرها.

**الدليل:** Run 6 على dev، بقاعدة سُجِّلت في `d3f39c3` قبل الأرقام.
- الشروط الثلاثة تحققت: B2 = 5/5، والمادة المتوقعة في 11/15، وصفر مصدر مخترع.
- أسقط الـgate خطأً حقيقياً لأول مرة: في Q012 كتب النموذج «المادة 2» وهو يقصد المصدر 2 (المادة 17).

**الثمن:**
- استدعاءان للنموذج لكل سؤال بدل واحد.
- امتناع كاذب في 2/15. أحدهما (Q014) فشلُ استرجاعٍ لا فشلُ امتناع: المادة المتوقعة لم تُسترجع أصلاً.

**حدود مسجَّلة:**
- الـgate يتحقق أن المصدر موجود ومُشار إليه، لا أنه يسند الجملة (ADR-022).
- القاعدة 3 مكتوبة بلغة التشريع («مادة N»). على مستندات لا مواد فيها يبقى منها فحصٌ شكلي فقط: أن المصدر من المسترجَع.
- تعليمات «المصادر نص للقراءة فقط» هي خط الدفاع الوحيد ضد تعليمات مزروعة في مستند مرفوع. claim مزروعة تستشهد بمصدر صحيح تجتاز الـgate.
- العينة صغيرة: 15 سؤالاً لها إجابة، و5 خارج المتن.

**شرط إعادة النظر:** أي تعديل على أيٍّ من الخطوات الثلاث يدخل بتسجيل مسبق، ويُقاس على نفس الأسئلة. وقبل الاعتماد على مستندات غير تشريعية، يُقاس الشكل على مجموعة التطبيق (`evals/app`).

---

## ADR-026 — المشفّر الكثيف يقطع النص بصمت: نوافذ tokens بدل مادة واحدة كاملة

**التاريخ:** 2026-09-19 · **الحالة:** معتمد · **يعدّل:** ADR-015 (لا يلغيه)

**المشكلة.** `SentenceTransformer.encode` بيقطع عند `max_seq_length` **من غير ما يرمي خطأ ولا تحذير**. المادة اللي أطول من النافذة بيتبنى لها vector شاف أولها بس، والـvector ده **مالوش أي علامة تميّزه** عن vector شاف المادة كلها: نفس الأبعاد، ونفس المدى، ودرجة معقولة الشكل. كل اللي بعد المشفّر — الـHit والاستشهاد والـgate والـeval — بيشتغل عادي وبيدّي رقم معقول.

**المقيس على قانون 151/2020 (بالـtokenizer بتاع e5-base نفسه):** 4 مواد من 56 فوق الـ512 token، و**2,906 حرف (8.3% من المتن) مش داخلة أي embedding**. الأسوأ `law-1` — مادة التعريفات — بتفقد 2,202 من 4,044 حرف، يعني **أكتر من نصها**، وجوه النص المفقود تعريفات «خرق البيانات» و«التسويق الإلكتروني» و«جهات الأمن القومي» و«الترخيص» و«التصريح».

**ليه ده أخطر من رقم استرجاع ضعيف:** الاسترجاع الضعيف بيبان في المقياس. ده **ما كانش بيبان**، لأن مجموعة dev كلها أدلتها في الجزء المرئي — اتفحص، وكل الكلمات المفتاحية للعشرين سؤال قبل حد القطع. يعني المجموعة كانت **عمياء عن العيب بالكامل**، والرقم 0.767 كان صادق ومضلِّل في نفس الوقت.

**القرار.** المادة اللي مش داخلة في نافذة واحدة بتتقسّم نوافذ متداخلة (تداخل 64 token)، وكل نافذة بتتضمّن لوحدها وبتحمل **رقم المادة الأصلية**. وقت البحث، درجة المادة = **أعلى** نوافذها.

**ليه أعلى نافذة مش متوسطها:** المتوسط بيخلي فقرات المادة الطويلة غير المتعلقة تخفّف النافذة اللي بتجاوب فعلاً، فالمادة القصيرة بتكسب **لأنها قصيرة** — وده عكس الغرض من التقسيم بالظبط.

**ليه مفيش قطع عند حدود الجمل:** التداخل بيحل نفس المشكلة بتركيب أبسط. الجملة اللي على الحد بتبقى كاملة في النافذة اللي بعدها، فمفيش داعي لقاعدة تانية تدوّر على نهايات الجمل.

**اللي ما اتغيرش بقصد:** المادة فضلت هي وحدة النتيجة. `Hit.id` و`Hit.snippet` و`recall_at_k` و`cite.gate` والاستشهاد — كلهم شايفين مواد زي الأول. النوافذ تفصيلة جوه المشفّر، ومش بتطلع منه.

**اللي بيحصل لما المشفّر ما يقدرش يقول حدّه** (stub في الاختبارات، أو `docindex.LazyEncoder` اللي بيأجّل تحميل torch بقصد): بيرجع لسلوك ADR-015 — مادة كاملة لكل vector. فمسار مستندات التطبيق **ما اتغيّرش**، وده متعمد: `LazyEncoder` بيتبني قبل ما النموذج يتحمّل، وقراءة الـtokenizer منه كانت هتلغي التأجيل ده.

**الدليل — قبل وبعد، على نفس المتن ونفس الأسئلة:**

| | قبل | بعد |
|---|---|---|
| probe الذيل (`evals/adversarial`) | 2/8 | **5/8** |
| ADV-T01 جهات الأمن القومي | رتبة 9 | **رتبة 1** |
| ADV-T02 مدة الترخيص | برّه أعلى 20 | **رتبة 1** |
| ADV-T04 تعريف الخرق | رتبة 7 | **رتبة 1** |
| **dense على dev (التكوين المعتمد)** | 0.767 / 0.588 | **0.767 / 0.588** |
| hybrid (RRF) | 0.600 / 0.600 | 0.600 / 0.530 |

**السببية مقيسة لا مفترضة:** تلات حالات اتحركت من «برّه» أو «رتبة 7-9» لـ**رتبة 1**، وتلات حالات تانية (T03، T07، T08) **ما اتحركتش خالص**. لو التقسيم كان بيرفع الأرقام عموماً كانوا اتحركوا كلهم. التلاتة اللي ثبتوا فشلهم **مش من القطع** — ضعف استرجاع عادي، ولسه مفتوح.

**الثمن المسجَّل:** الـMRR بتاع hybrid (RRF) نزل من 0.600 لـ0.530. درجات المواد بقت من أحسن نافذة، فترتيب المواد غير المستهدفة جوه قايمة الكثيف اتغيّر شوية، والدمج ورث التغيير. ده **سؤال واحد نزل رتبة** على 15 سؤال — تحت عتبة السؤالين اللي المشروع نفسه بيقول إن أقل منها ما يتقراش (Run 4). وhybrid **مش التكوين المعتمد** أصلاً (Run 2 استبعده لصالح الكثيف وحده)، فالأثر على التطبيق: صفر.

**حد في المنهج، مسجَّل صراحةً:** الـprobe اتكتب واتقاس **قبل** الإصلاح، وده الترتيب الصح. لكن **عتبة قبول رقمية ما اتسجلتش قبل التشغيل** زي Run 4-6. القاعدة المطبقة («التكوين المعتمد ما ينزلش، والـprobe يطلع») مكتوبة بعد رؤية الأرقام، فهي أضعف كدليل من قواعد الـruns دي. القارئ ما يقدرش يتأكد إني ما كنتش هسمّي نتيجة تانية نجاح.

**الـcache:** أي cache اتكتب قبل النوافذ **بيتبني من أول وجديد** — مفيهوش خريطة الصفوف، والقراءة بتفشل وترجّع «أعد البناء». ده مقصود: الـvectors القديمة هي المقطوعة اللي الملف ده موجود عشان يبطّل استخدامها.

**حدود:**
- نافذة واحدة بالظبط عند 512 token هي الحالة اللي ما اتقاستش مباشرةً؛ الاختبارات بتغطي الحدود بـtokenizer صناعي مش بالنموذج الحقيقي.
- مستند تشريعي مرفوع فيه مادة أطول من النافذة **لسه بيتقطع بصمت**، لأن مسار التطبيق بيستخدم `LazyEncoder`. ده متسجّل كعمل باقٍ، مش متصلَّح هنا.
- الرقم 8.3% عن **المتن ده** (النسخة المعاد نشرها، ADR-021). على متن الجريدة الرقم هيختلف.

**شرط إعادة النظر:** تغيير `WINDOW_OVERLAP_TOKENS` أو المشفّر بيلغي مقارنة الأرقام دي ويطلب إعادة قياس.

---

## ADR-027 — طيّ صور الحروف في المسار الكثيف: **مرفوض بالقياس**، والضعف يفضل مفتوح

**الحالة:** مرفوض (2026-09-19). ADR-015 يفضل ساري: المسار الكثيف بياخد نص المتن ونص السؤال **بإملائهم الأصلي**.

**السياق.** probe الإملاء (Run 8) قاس حاجة مقلقة: إعادة كتابة أسئلة dev زي ما بيكتبها مصري عادي — «الافراد» بدل «الأفراد»، «شركه» بدل «شركة» — **حرّكت رتب 10 من 15 سؤال**، و3 منهم خرجوا من أعلى 5 خالص. يعني الرسم الإملائي كان بيتحكم في الترتيب.

الفجوة كانت حقيقية: المشروع كان عنده مطبّعان بس. `search_normalize` عدواني (بيشيل السوابق ويهرس الترقيم) وADR-015 بعّده عن النموذج عن قصد، و`evaluation_normalize` محافظ عشان التقييم. مفيش حاجة في النص.

**الإصلاح اللي اتجرّب.** `orthographic_normalize`: يطوي صور الحروف اللي بتختلف بعادة الكيبورد بس (مقاعد الهمزة، التاء المربوطة، الألف المقصورة)، ويسيب الكلمات والسوابق والترقيم زي ما هي — فالناتج لسه بيتقرا عربي. اتطبّق على **الجهتين** (كل مادة + السؤال).

**قاعدة القبول اتسجّلت في EVAL.md قبل كتابة الكود** — تصحيحاً لضعف منهجي متسجّل في ADR-026، اللي عتبته اتكتبت بعد رؤية الأرقام.

**النتيجة.**

| | قبل | بعد |
|---|---:|---:|
| **«زي ما هي» — dev، التكوين المعتمد** | **0.767 / 0.588** | **0.667 / 0.536** |
| «الكل معاً» | 0.633 / 0.482 | 0.667 / 0.536 |
| أسئلة اتحركت رتبها بالرسم | 10 من 15 | **0** |
| خرجت من أعلى 5 | 3 | **0** |

**الثبات اتحقق بالكامل** — صفر حركة في كل صور الكتابة الخمسة. التشخيص كان صح. والثمن كان **0.100 recall@5** على الكتابة السليمة، وده كسر الشرط الأول في القاعدة المسجّلة («أي نزول = رفض»).

**والحساب بيأكد القرار، مش بس القاعدة.** لو نسبة الأسئلة المكتوبة برسم سليم = p، التكوينان بيتساووا عند **p ≈ 0.25**. يعني لو أكتر من ربع الأسئلة مكتوبة صح، الطيّ **خسارة صافية** — ومعظم الكتابة الواقعية مش هتجمع الصور الأربعة مع بعض زي أسوأ حالة في القياس. p نفسها **مش مقيسة**: مفيش أسئلة مستخدمين حقيقيين لحد دلوقتي.

**ليه الطيّ خسر، وده أنفع من النتيجة نفسها.** المشكلة مش إن الطيّ غلط — المشكلة إنه **بيتطبّق على الجهتين بالتساوي**، فبيدفع تكلفة فقد التمييز حتى لما السائل كاتب صح. الضرر مقيس على حالات بعينها: ADV-T04 من #1 لـ#3، وADV-T05 من #3 لـ#5، وADV-O03 عدّى بس برتبة #2 بدل #1.

**اللي اتساب في الكود بعد الرجوع، ومبرراته:**
1. `orthographic_normalize` في `normalize.py` — مش كود ميت: `search_normalize` متعرّف فوقه، فالطيّات المشتركة بين الاتنين ما تقدرش تفترق. وهو اللي الاختبار بيستخدمه عشان يثبت إن المرفوض فعلاً مرفوض.
2. `fold` كـparameter في `DenseIndex` و`encode_query`، افتراضه `verbatim`. السبب **مش** التوسّع المستقبلي: الـcache بيسجّل اسم الطيّ اللي الـvectors اتبنت بيه. من غير الاسم ده، فهرس اتبنى تحت طيّ هيتقرا بصمت لأسئلة غير مطوية وكل مقارنة تبقى غلط شوية من غير أي إشارة. الحارس ده اتجرّب فعلاً عند الرجوع: رفض الـcache المطوي وأعاد البناء لوحده.

**الضعف مفتوح، مش متصلَّح.** سؤال متكتب «الافراد» لسه ممكن يفوّت «الأفراد». ADV-O03 لسه بيسقط. القرار هنا هو إن **الإصلاح ده** مش مقبول، مش إن المشكلة مش موجودة.

**الاتجاه اللي الأرقام بتشاور عليه (غير مقاس):** فهرسة كل مادة بالصورتين — الخام والمطوية — وتضمين السؤال بالصورتين، والدرجة = الأعلى. الخام-مع-الخام بيحافظ على 0.767، والمطوي-مع-المطوي بيمسك الكتابة العامية، والتكلفة بتتدفع بس لما تلزم. آلية `rows` من ADR-026 بتستحمل ده من غير تركيب جديد. **ده فرضية، وتحتاج قاعدة قبول جديدة تتسجّل قبل أي كود.**

**شرط إعادة النظر:** أول ما يبقى في أسئلة مستخدمين حقيقيين تقيس p، أو لما تتجرّب صيغة بتفصل تكلفة الطيّ عن الكتابة السليمة.

---

## ADR-028 — فهرسة المادة بصورتين ودمجهما بالـmax: **مرفوض بالقياس**، وخط التطبيع مقفول

**الحالة:** مرفوض (2026-09-19). ADR-015 يفضل ساري. وده **تاني** رفض للضعف الإملائي بعد ADR-027، والتالت في خط التطبيع كله.

**السياق.** ADR-027 رفض طيّ الجهتين لأنه بيدفع تكلفة فقد التمييز حتى لما السائل كاتب صح. الاقتراح هنا كان يفصل التكلفة دي: كل مادة تتضمّن **مرتين** — بإملائها الأصلي وبصورتها المطوية — والسؤال يتضمّن مرتين، ودرجة المادة = **أعلى زوج متطابق**: `max(خام·خام, مطوي·مطوي)`. الخام-مع-الخام يحافظ على 0.767، والمطوي يمسك الكتابة العامية، والتكلفة تتدفع بس لما تلزم.

**التقاطع مستبعد بالتصميم، مش بالصدفة.** مقارنة سؤال خام بمادة مطوية هي **بالظبط** المشكلة؛ إضافتها كمرشّح بترجّعها من باب التسعير. ده متثبّت باختبار ‏(`test_a_query_vector_never_scores_against_the_other_spelling`) اتجرّب بطفرة: تنفيذ بيسجّل التقاطع بيعدّي الترتيب ويسقط الدرجة — 1.0 بدل 0.0.

**قاعدة القبول اتسجّلت في EVAL.md قبل الكود** (Run 10)، ومعاها **الخطر متوقَّع بالاسم**: «الـmax على مرشحين أكتر بيرفع الدرجات بشكل غير متساوٍ».

**النتيجة.**

| | HEAD | مزدوج |
|---|---:|---:|
| **«زي ما هي»** | **0.767 / 0.588** | **0.700 / 0.549** |
| «الكل معاً» | 0.633 / 0.482 | 0.667 / 0.536 |
| «خرجت من top-5» (أسوأ صورة) | 4 | 1 |

الشرط 1 سقط، والشرط 2 سقط (المطلوب كان «خرجت» = 0 لكل صورة و recall@5 ≥ 0.733؛ الفعلي 1 في تلات صور و0.667 في تلاتة).

**ليه سقط — وده الجزء اللي يستاهل يتقرا.** الدعوى «الخام-مع-الخام محفوظ» **صح على الدرجات**:

```
النصف الخام من المزدوج  مقابل  المفرد : فرق 1.79e-07  (ضوضاء float32)
مواد درجتها نزلت بعد الإزدواج          : صفر
```

و**غلط على الرتب**:

```
law-8 (إجابة Q002)  lift = +0.0069   المتوسط = +0.0211   الأقصى = +0.0435
10 مواد درجتها الخام أقل من law-8 عدّت عليها بالطيّ وحده
Q002: #3 -> #13      Q014: #12 -> برّه العشرين
```

كل مادة بتاخد دفعة من نسختها المطوية، والدفعات **مش متساوية**، والإجابة الصح خدت واحدة من أصغرها. **الترتيب بيقرا الفروق مش القيم، فرفع الكل بمقادير مختلفة = إعادة ترتيب.**

**والتعميم اللي بره الحالة دي:** «زوّد قناة وادمج بالـmax» **ما ينفعش يكون Pareto** أبداً. القناة الجديدة بتدي كل عنصر قدر مختلف، والاختلاف ده وحده بيعيد الترتيب — حتى لو مفيش أي عنصر نزلت درجته. أي اقتراح دمج جاي في المشروع ده (نموذج تاني، تمثيل تاني، قناة ثالثة) لازم يرد على السؤال ده قبل ما يتكتب.

**المقارنة الكاملة، ونقطة التعادل:**

| | زي ما هي | الكل معاً | نقطة التعادل مع HEAD |
|---|---:|---:|---:|
| **HEAD (خام وحده)** | **0.767** | 0.633 | — |
| ADR-027 (طيّ الجهتين) | 0.667 | 0.667 | p ≈ 0.25 |
| ADR-028 (مزدوج) | 0.700 | 0.667 | **p ≈ 0.34** |

المزدوج أحسن من طيّ الجهتين، والاتنين أسوأ من HEAD طالما أكتر من تلت الأسئلة مكتوبة برسم سليم.

**التكلفة، مقيسة برضه:** ×1.46 في الزمن، وضعف في حجم الـcache (185KB ← 369KB) والصفوف (61 ← 122). مش سبب الرفض. **وحد في التسجيل المسبق نفسه:** شرط التكلفة اتكتب برقم مطلق طلع تابع لطريقة القياس؛ النسبة هي اللي لها معنى. مسجّل كخطأ في صياغة القاعدة، مش في النتيجة.

**الرجوع كامل.** الآلية كلها اتشالت — `row_folds`، القناع لكل طيّ، `search_with_vectors`، فحوص الـcache الزيادة، و23 اختبار. سيبان 50 سطر واختباراتها لتصميم مرفوض هو بالظبط التعميم الاستباقي اللي معايير المشروع بترفضه. اللي فضل: سطرين تعليق في `search_with_vector` و`_fold_name` بيقولوا للي جاي إيه اللي اتجرّب وليه وقع.

**القرار على خط البحث كله:** الضعف الإملائي **مقفول على مستوى التطبيع**. التلات محاولات المعقولة (طيّ السؤال وحده · طيّ الجهتين · قناتين بالـmax) بتصطدم بنفس الحقيقة: **e5 بيقرا الرسم كإشارة، والتلاعب بالرسم بيحرّك إشارات تانية معاه.** فتح الملف ده تاني محتاج **واحد من اتنين**، مش محاولة رابعة في نفس الطبقة:
1. **أسئلة مستخدمين حقيقية** تقيس p — نسبة الكتابة السليمة. كل الحسابات فوق متوقفة عليها، ومفيش منها ولا رقم.
2. **نموذج متدرّب على تنوّع إملائي** بدل تطبيع فوق نموذج مش متدرّب عليه.

**شرط إعادة النظر:** وجود (1) أو (2). غير كده الإجابة متقاسة مرتين.

---

## ADR-029 — OCR للمستند الممسوح في مسار رفع التطبيق: **مرفوض بالقياس**، وdeepseek-ocr يُسجَّل كمرشّح
**2026-09-19 · مقبول** · نتيجة Run 11 في `EVAL.md`

**السياق:** طُلب دعم OCR لمستند ممسوح يُرفع على التطبيق (`قانون الإجراءات الجنائية 174/2025` — 137 صفحة، **صفر حرف نص**). التطبيق بيرفضه النهاردة بـ`NoTextLayer`، ورسالته مكتوب فيها حرفياً «a scanned PDF needs OCR first».

**والأهم: شرط إعادة نظر تحقّق بهدوء.** ADR-021 كتب من شروط سقوطه **(ج): «متى صار المشروع يقتبس نص المواد للمستخدم»**. طبقة التطبيق (ADR-023) بتقتبس نص المصدر في كل استشهاد وشغّالة إنتاجياً من اليوم. الشرط تحقّق قبل ما حد ياخد باله، **وهو اللي يجعل السؤال مشروعاً هنا** بعد ما ADR-019/020/021 رفضوا OCR كمتن مرجعي.

**لماذا لم تُنقل البوابة كما هي.** عتبة `ocr_gate` (0.99، صفر تسامح) مبنية في ADR-020 على أن أرقام المواد مفتاح أساسي: بتقطّع المتن، وهي ground truth كل سؤال، وكل إحالة بتُحَل بيها. في مستند يرفعه المستخدم، **سببان من التلاتة يسقطان** — التقطيع بيتدهور لـ`page_chunks` وده ظاهر ومسجَّل في `kind`، ومفيش ground truth ولا حل إحالات. **والتالت باقٍ بكامل قوته:** رقم غلط جوّه إجابة يقراه المستخدم كأنه القانون. فاتسجّلت قاعدة من تلات شروط **قبل أي قياس** (Run 11): ≥0.99 يدخل بلا تمييز · ≥0.90 يدخل بعلامة توصل المستخدم · تحته يُرفض.

**ما قيس:** حقيقة أرضية جديدة (`evals/ocr/law174_digit_ground_truth.json`) — 29 رمزاً على تلات صفحات، مقروءة بالعين من الصور قبل تشغيل أي محرّك. سبعة تكوينات، أربعة محرّكات:

| | دقة الأرقام | حرج |
|---|---:|:--:|
| deepseek-ocr 3.3B | **82.8%** | **0 ✓** |
| surya-ocr 0.17.1 (تلات تكوينات) | 82.8% | 1 ❌ |
| easyocr 1.7.2 | 69.0% | 4 ❌ |
| tesseract 5.5.0 (تكوينان) | 17.2% | 3 ❌ |

**القرار: مرفوض.** أحسن درجة 82.8%، تحت عتبة «بعلامة». التطبيق يفضل يرفض الـPDF الممسوح بـ`NoTextLayer` زي ما هو، **ولا يتغيّر فيه سطر واحد**.

**السبب.** surya قرا عنوان الغلاف «قانون رقم **١٧٤**» كـ«١٧**٦**» — على خط عريض كبير عالي التباين بلا أي تداخل من العلامة المائية. نظام بيقول للمستخدم «قانون رقم ١٧٦» أسوأ من نظام بيقول «مش قادر أقرا الملف ده». والرفض الحالي **صادق**: بيفشل بصوت عالٍ، ورسالته بتقول السبب الحقيقي.

**ثلاث نتائج تُغلق أبواباً كانت مفتوحة:**

1. **الدقة ليست العلّة.** المسح 96 DPI والرفع لـ300 DPI ما حرّكش الإجمالي عند أي محرّك (surya 82.8%←82.8%، tesseract 17.2%←17.2%). العجز في نموذج التعرّف العربي — نفس شكل نتيجة ADR-020 مع كاشف Paddle.
2. **tesseract مغلق رغم كونه الأنسب معمارياً.** هو الوحيد اللي بيدخل حاوية التطبيق بسطر `apt-get` واحد وهو **أسرع 30 ضعف**، وهو الأسوأ بفارق هائل (17.2%، بيخترع `701172`).
3. **القيد الزمني مستقل عن الجودة.** `EXTRACTION_BUDGET_SECONDS = 300` والرفع متزامن؛ 137 صفحة = 4.7–7.7 ساعة. **حتى لو عدّى محرّك، الـOCR لا يمكن أن يتم داخل طلب الرفع** — يحتاج شغلاً غير متزامن أو خطوة منفصلة. ده شكل معماري، مش نتيجة قياس، ومسجَّل كده.

**ملاحظة مسجَّلة ولم يُبنَ عليها — عمداً.** deepseek-ocr سجّل نفس 82.8% بسلوك مختلف كلياً: **صفر إخفاق حرج**، كل ترويسات المواد صحيحة، وقائمة «المادتين ١٧٤، ١٧٥» كاملة بفاصلتها. **كل أخطائه الخمسة في أثاث الصفحة** (ترويسة الجريدة وأرقام الصفحات) — و`ocr_text.py` بيشيل الأثاث ده أصلاً. لكن **لم يُعَد حساب المقياس على المتن وحده**، لأن ده بالظبط شكل الخطأ اللي المشروع سجّله على نفسه في Run 8: عتبة تُكتب بعد رؤية الأرقام. وموضوعياً العينة لا تحتمله: 17 رقم متن، وعكس الترتيب اللي أنتج `٢١` من `١٢` كان ممكن يقع على رقم مادة بنفس السهولة ويعطي «مادة (٢١)» سليمة الشكل ومشيرة للمادة الغلط للأبد.

**ومع ذلك deepseek-ocr هو المرشّح الأول لو فُتح الملف تاني**، لسبب معماري فوق درجته: بيشتغل على **Ollama**، والتطبيق بيكلّم Ollama أصلاً (ADR-024). يعني **بلا حاوية إضافية وبلا بيئة conda وبلا تعارض تبعيات** — وهي العقبة اللي أسقطت مسار surya في ADR-020 من الأساس.

**شروط إعادة النظر:**
1. **نسخة رقمية بطبقة نص** — تُلغي السؤال كله.
2. **محرّك يعدّي 0.90 على هذه الحقيقة الأرضية** بلا تدخّل يدوي.
3. **قياس جديد يفصل أثاث الصفحة عن المتن**: حقيقة أرضية على صفحات أكتر، كل رمز فيها موسوم `furniture` أو `content`، وقاعدة قبول مسجَّلة قبل تشغيل أي محرّك. ده الشكل الوحيد اللي ملاحظة deepseek تتفعّل بيه، ولازم يشمل `qwen2.5vl:3b` اللي **لم يكتمل قياسه** هنا (نفد ذاكرة الجهاز) ويُسجَّل كقياس ناقص لا كنتيجة.

---

## ADR-030 — deepseek-ocr يسقط كمرشّح: الشرط التالت في ADR-029 استُهلك، والفرضية انكسرت على صفحات محجوزة
**2026-09-19 · مقبول** · نتيجة Run 12 في `EVAL.md`

**السياق.** ADR-029 رفض OCR في مسار الرفع، وسجّل ملاحظة **لم يُبنَ عليها عمداً**: `deepseek-ocr` سجّل 82.8% بصفر إخفاق حرج، و**كل** أخطائه الخمسة وقعت في أثاث الصفحة اللي `ocr_text` بيشيله أصلاً. إعادة حساب المقياس على المتن وحده وقتها كانت هتبقى عتبة تُكتب بعد رؤية الأرقام — نفس الخطأ المسجَّل على المشروع في Run 8. فاتسجّل شرط إعادة نظر (ج): **قياس جديد يفصل الأثاث عن المتن، على صفحات أكتر، بقاعدة مكتوبة قبل التشغيل.** ده اللي حصل.

**كيف حُفظت الاستقامة هذه المرة.** القاعدة اتكتبت في `EVAL.md` **قبل** بناء الحقيقة الأرضية وقبل ما أي محرّك يشوف أي صفحة جديدة: تلات شروط (≥0.99 بلا علامة · ≥0.95 **وصفر مخترَع** بعلامة · غير كده رفض)، وحد أدنى **8 صفحات جديدة و50 رمز متن**، والحكم **على الصفحات الجديدة وحدها** لأن p00/p09/p49 هي اللي ولّدت الفرضية. الحدّين دول مفروضين بـ`test_the_run12_ground_truth_is_held_out_and_meets_the_pre_registered_minimum`، مش متروكين للنثر — النثر هو اللي خلّى «0 remaining broken words» في ADR-013 يعيش سنة وهو غلط.

**تحقّق سبق القياس:** prompt الـRun 11 ما كانش متسجّلاً. بدل افتراضه، أُعيد تشغيل `p00` بـ`<image>\nFree OCR.` وطلع **مطابقاً حرفياً** لمخرَج Run 11 (11 رمزاً). القياسان على أرضية واحدة.

**ما قيس.** `evals/ocr/law174_page_roles_ground_truth.json` — 8 صفحات محجوزة، 85 رمزاً (**53 متن، 32 أثاث؛ الأثاث 37.6% من المقياس القديم**)، كلها مقروءة بالعين قبل أي تشغيل.

| | كل الصفحة | **المتن وحده** | حرج | مخترَع |
|---|---:|---:|:--:|:--:|
| صفحات Run 11 (لا تحكم) | 82.8% | **100%** | 0 | 0 |
| **الصفحات المحجوزة (الحكم)** | 82.4% | **86.8%** | **3** | **11** |

**القرار: رفض (الشرط 3).** ADR-029 يفضل ساري بلا تعديل، و**deepseek-ocr ما بقاش المرشّح الأول** — ترشيحه كان مبنياً على ملاحظة انكسرت. الشرط (ج) استُهلك: القياس اللي كان مطلوباً اتعمل، وجاوب بلا.

**السبب — الفرضية كانت إفراطاً في الملاءمة، والخطر المسجَّل هو اللي وقع.** 100% على الصفحات اللي ولّدت الفرضية، 86.8% على صفحات جديدة. و`p035` بتوضّح الشكل: المواد `١٢٥ ١٢٦ ١٢٧ ١٢٨` طلعت `١٣٥ ١٣٦ ١٣٧ ١٣٨` — **نفس الإبدال أربع مرات متتالية**، وكلها مواد حقيقية في نفس القانون. و`p108`: «من المادة **٤١**» بدل `٤١٤` — والمادة 41 موجودة. جملة عربية قانونية سليمة تماماً بتشاور على مادة غلط، للأبد. عادة عكس الترتيب اللي Run 11 شافها في الترويسة (`٤٥`←`٥٤`) ظهرت هنا في المتن (`٧٢`←`٢٧`): **العادة خاصية في النموذج، ووقوعها على الأثاث كان صدفة العيّنة.**

**النتيجة السلبية دي هي المنتج.** القاعدة اتطبّقت زي ما اتكتبت. لو اتقاس المتن وحده بعد Run 11 من غير حجز صفحات، الرقم كان 100% والقرار كان اعتماد — على فرضية مكوّنة من تلات صفحات وعُمرها انكسر على تمانية.

**ما لم يتغيّر:** القيد المعماري في ADR-029 مستقل عن الجودة ولسه قائم — `EXTRACTION_BUDGET_SECONDS = 300` والرفع متزامن، و137 صفحة بـ**182.8ث للصفحة وسطاً** (مقيس، المدى 150.4–226.8) = **7.0 ساعات**. حتى محرّك مثالي ما بيعديش في طلب رفع.

**نتيجة جانبية، اتصلحت.** القياس كشف إن `ocr_text` بيفوّت ترويسة أعاد النموذج بناءها، وجذرها إن `يولية` كانت الشهر الوحيد في `FURNITURE_WORDS` — قاعدة معايرة على الجريدة اللي اتكتبت في مواجهتها. اتصلحت بقواعد لا بعتبة، ومقيسة على 919 سطر. **إعادة تسجيل Run 12 بالمسطرة المصلَّحة ما بتحركش الحكم**: 46/53 = 86.8% وتلات إخفاقات حرجة زي ما هي؛ المخترَع وحده ينزل 11←7. التقريران محفوظان جنب بعض في `evals/ocr/run12/` لأن تغيير المسطرة بعد رؤية النتيجة هو شكل التقطيع البَعدي اللي المشروع يرفضه — ولو كان الإصلاح يقلب الحكم، كان لزم تشغيل جديد بتسجيل جديد.

**شروط إعادة النظر** — نفس شروط ADR-029، ناقص الشرط (ج) اللي استُهلك. ويُضاف:
4. `qwen2.5vl:3b` **لسه غير مقيس** — لم يكتمل للمرة التانية بنفس الآلية (العملية تتمدد لـ~6 GB على صورة 800×1000 وتدخل thrashing؛ Run 12 بدأ بـ7.7 GB حرة بدل ~2، فضيق الذاكرة الابتدائي **ليس السبب**). قياس ناقص لا نتيجة، ولا يُحسب لصالح `deepseek` ولا ضده. التفاصيل في `EVAL.md`.

---

## ADR-031 — تحميل المُرمِّز مرة واحدة ليس تكلفة الطلب الأول: تسخين عند البدء، وميزانية T11 تبدأ بعده
**2026-09-19 · مقبول**

**العَرَض.** أول تشغيل إنتاجي للحاوية: `/api/chat` ردّ **429 بعد 244.9 ثانية**، ورسالته «waited 244.9s for a generation slot, past the 120s limit». **مفيش متصل واحد كان في الطابور، والقفل كان فاضي طول الوقت.**

**الجذر.** `LazyEncoder` بيحمّل نموذج e5 (1.1 GB) عند أول `encode`، وأول `encode` بيحصل جوّه `library.search`، اللي بيحصل جوّه `ask` **بعد** `started = perf_counter()`. فـ`waited_s = perf_counter() - started` كان بيشمل الاسترجاع كله ومعاه التنزيل. **المهلة كانت بتقيس الفترة الغلط، والرسالة كانت غير صحيحة حرفياً.**

**التوتر اللي اتحل.** T11 (في `PROJECT-REVIEW-AR.md`) طلب صراحةً «**ميزانية end-to-end تشمل الانتظار**»، وده اللي الكود بينفّذه واللي `test_a_caller_that_already_waited_past_the_deadline_never_reaches_the_model` بيثبّته. بينما التعليق فوق `MAX_WAIT_SECONDS` والرسالة الموجّهة للمستخدم بيوصفوا **انتظار الطابور** وحده. فالحل مش اختيار طرف:

> **تحميل نموذج مرة واحدة لعمر العملية ليس شغل هذا الطلب.** هو بدء التشغيل، والطلبات اللي بعده كلها بتستفيد منه. تحميله على حساب أول واحد صدفةً هو خطأ تصنيف، تحت **أي** من القراءتين.

الميزانية فضلت end-to-end كما طلب T11. اللي اتغيّر هو **متى تبدأ**: `ask` بينتظر جاهزية المُرمِّز **قبل** ما يشغّل ساعته.

**التغيير — تلات نقط:**
1. `LazyEncoder.warm()` — تحميل صريح، idempotent، تحت نفس القفل. `encode` بقى `self.warm()` ثم `self._model.encode(...)`، بنفس دلالات القفل بالظبط.
2. `Library.warm_encoder()` — بتنادي `warm` لو المُرمِّز بيعرفها (`getattr`، نفس الأسلوب الدفاعي المستخدم أصلاً لـ`.close` و`.calls`؛ المُرمِّز المحقون في الاختبارات أي حاجة عندها `.encode`).
3. `webapp` بيسخّن عند البدء **في خيط daemon**. مش inline: التنزيل دقائق و`HEALTHCHECK` مهلته `start-period=30s` — تسخين حاجز كان هيخلّي Docker يعلن الحاوية غير سليمة وهي بتعمل الصح بالظبط. وdaemon عشان العملية تقدر تخرج لو حد وقف التطبيق وسط التنزيل.

**ما لم يتغيّر.** الاسترجاع البطيء لسه بيستهلك الميزانية — هو شغل الطلب نفسه مهما طال. الاختبار اللي بيحرس ده باقٍ بلا تعديل، واتضاف جنبه `test_slow_retrieval_still_spends_the_budget` عشان الحد بين الحالتين يبقى مرئي في مكان واحد. وحد الدخول (`MAX_CONCURRENT_GENERATIONS = 3`) ما اتلمسش.

**التحقق — بساعة حقيقية، لا بـ`perf_counter` مرقّع.** تحميل 2.0ث وميزانية 1.0ث (نفس شكل العطل المبلَّغ): قبل الإصلاح `TooBusy`، بعده `answered` في 2.0ث. وعبر `TestClient` بتطبيق حقيقي: `GET /` (مسار الـhealthcheck) بيرد **200 في 0.00ث والمُرمِّز لسه بيحمّل**، وأول رفع بعد التسخين **0.16ث** بدل ما يدفع التحميل.

**التحقق على الحاوية نفسها، ببدء بارد حقيقي.** اتمسح volume الكاش (`arabic-legal-rag_hf-cache`)، اتعمل `docker compose up -d`، وأول سؤال اتبعت **بعد 2.4 ثانية من فتح المنفذ** — والنموذج لسه بينزّل. السكربت والناتج في `evals/app/coldstart_check.py` و`coldstart_result.json`.

| | ساعة الحائط | `timings_ms.total` (ساعة الميزانية) |
|---|---:|---:|
| **بارد** (الحالة المبلَّغ عنها) | **255.8ث** | **11.7ث** |
| دافئ (نفس السؤال بعدها) | 11.6ث | 11.5ث |

**الفرق بين العمودين هو الإثبات.** في الدافئ الساعتين متطابقتين. في البارد بيفرقوا **~244 ثانية** — وهو التنزيل، ونفس رقم البلاغ الأصلي (244.9ث) تقريباً. الميزانية 120ث، و11.7 تحتها بمسافة واسعة، فالرد كان **HTTP 200 · `answered`** بإجابة مستندة بدل 429.

**والنصف التاني من الإصلاح اتقاس كمان:** أثناء الـ255.8 ثانية اتبعت **126 فحصاً على `/`** (مسار الـ`HEALTHCHECK`): **صفر ردّ غير-200، وأبطأ واحد 0.965ث**. قرار «خيط خلفية لا inline» متحقَّق منه بالقياس. الحاوية عدّت لـ`healthy` وهي بتنزّل.

**الأثر على المستخدم.** أول طلب بعد `docker compose up` على كاش بارد: كان 429 بعد ~4 دقايق، بقى بيتخدم عادي — والتنزيل بيحصل في الخلفية من لحظة البدء، فغالباً خلص قبل ما المتصفح يتفتح.

**ما لم يُعالَج، وعن قصد:** الحاوية بتقول «healthy» وهي لسه بتسخّن. فصل readiness عن liveness تغيير في سلوك النشر، وأوسع من العطل المطلوب إصلاحه. مسجَّل كبند مفتوح.

---

## ADR-032 — `ocr_text` يشيل رندرة المحرّك كاملةً لا وسوم HTML وحدها: markdown كان بيبلع مواد بصمت
**2026-09-19 · مقبول**

**كيف ظهر.** بعد ADR-030 (رفض OCR في مسار الرفع)، طُلب تشغيل `ocr-to-raw` + `ingest` على المستند الممسوح يدوياً. قبل الالتزام بسبع ساعات، شُغِّل المسار على الـ11 صفحة المقروءة فعلاً. `ingest` قال **نجاح**: 38 مادة، `extraction problems: none`. والحقيقة: من **50 ترويسة مادة** على الصفحات دي، **16 ضاعت بالكامل** و4 دخلت برقم غلط.

**الجذر — عطل تاني مستقل تماماً عن دقة الأرقام.** `deepseek-ocr` بيكتب بعض ترويسات المواد **markdown**: «`### مادة (٦٦)`». و`strip_markup` كان بيشيل وسوم **HTML** وحدها (اتكتب في مواجهة surya، اللي بيطلّع `<b>مادة</b>`). فـregex ترويسة المادة في `ingest` ما بيطابقش السطر، **والمادة مش بتتقطّع ومش بتدخل المتن أصلاً**. صفر أخطاء. فجوة في تسلسل 800 مادة مش حاجة حد بيلاحظها وهو بيقرا.

**ده مش اللي Run 11 و12 قاسوه.** دول قاسوا **الأرقام**؛ ده عطل في **التقطيع**. ظهر لأن المسار اتشغّل فعلاً، لا لأنه اتقرى.

**التغيير.** `strip_markup` بقى بيشيل HTML **ثم** بادئات markdown في أول السطر: ترويسات `#`..`######` ونقاط `-`/`*`/`+`. الترتيب مقصود — المحرّك بيقدر يطلّع الاتنين في سطر واحد (`### <b>مادة</b> (٦٦)`)، والوسم لازم يروح قبل ما البادئة تبقى في أول السطر.

**النطاق ضيّق عن قصد، ومقيس لا مفترَض.** مسح للمستند طلّع **14 سطر ترويسة و8 أسطر نقاط من 285**، وصفر bold أو italics أو جداول أو code fences أو blockquotes — فاتصلح اللي موجود فقط. والعلامتان بيطلبوا **مسافة بعدهم**، فـ`#٦٦` وشَرطة وسط جملة بيفضلوا كما هم: الوحدة دي بتشيل رندرة المحرّك، **ما بتحررش متن السجل**.

**القياس على كل خرج OCR حقيقي في الريبو** (1106 سطر، تلات محرّكات): **24 سطر اتغيّر، كلهم في الصالح** — 14 ترويسة اترجعت، 8 نقاط اتشالت، وسطرين في جريدة 2020 شالوا شَرطة كانت زيادة على بند مرقّم. صفر ارتداد.

**الأثر المقيس على الـ11 صفحة:**

| | قبل | بعد |
|---|---:|---:|
| ترويسات وصلت صح | 34/50 (68%) | **46/50 (92%)** |
| ضاعت بصمت | 16 | **4** |
| دخلت برقم غلط | 4 | 4 |

**الأربعة الباقية (`١٢٥-١٢٨` ← `١٣٥-١٣٨`) خارج نطاق الوحدة دي**: فساد أرقام في المحرّك، وهو نفس الفشل اللي ADR-030 رفض عليه المسار الآلي. الإصلاح ده **لا يغيّر شيئاً في ADR-029/030**: الرفع من التطبيق لسه بيرفض المستند الممسوح، واللي اتغيّر هو أمانة المسار اليدوي لما حد يختار يشغّله.

**درس متكرر.** ADR-031 وADR-032 اتكشفوا في نفس اليوم بنفس الطريقة: **تشغيل المسار الحقيقي بدل قراءته**. الأول ظهر في أول تشغيل إنتاجي للحاوية، والتاني في أول تشغيل فعلي لـ`ocr-to-raw`+`ingest` على مستند جديد. الاتنين كانوا بيفشلوا بصمت، والاتنين كان عندهم اختبارات خضرا.
