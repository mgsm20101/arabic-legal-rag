# PROJECT-STATE

سطر واحد أسبوعياً. الغرض: استئناف الشغل بعد انقطاع من غير إعادة بناء السياق في الدماغ.

---

## سجل الساعات (بدأ 2026-09-14)

ميزانية P1: ‏40 ساعة، والسقف 52 (PRD §9). **الإنفاق قبل 2026-09-14 مش مسجّل**، فالسجل بيبدأ من طبقة الـdemo (ADR-023، التقدير ~17 ساعة).

| التاريخ | المرحلة | المقدَّر (س) | الفعلي (س) |
|---|---|---:|---:|
| 2026-09-14 | 0 — تجهيز: ADR-023، الاعتماديات، Ollama | 1.0 | 0.2 |
| 2026-09-14 | 1 — Run 4: تغيير النموذج (Ollama + `--model`، ‏3 مرشحين، مراجعتين) | 2.5 | 2.0 |
| 2026-09-14 | 3 — eval التطبيق (مستند اصطناعي + 15 سؤال + PDF) | 1.5 | 0.6 |
| 2026-09-14 | 2 — Run 5 + Run 6: شكل الإجابة، والـgate، وخطوة الامتناع | 2.5 | 6.5 (منها ~1.5 في مشكلة Docker) |

**بعد المراحل 0–3:** ‏9.3 ساعة مقابل 7.5 مقدَّرة، يعني زيادة 24%. خط القطع في ADR-023 بيتطبّق عند 25%. الباقي من التقدير (المراحل 4–6): ‏9.5 ساعة.

---

## 2026-09-07 — أسبوع 1

**اتعمل:**
- هيكل الريبو + `python tasks.py eval` بيشتغل من fresh clone (المعيار المتفق عليه للأسبوع الأول ✅).
- `PRD.md` v1.0 — النطاق، تقسيم dev/val/locked، معايير قبول M1 و M2 بأرقام.
- `DECISIONS.md` — 6 ADRs (eval-first، التقطيع بالمادة، متن واحد، فصل التطبيع، التحقق الآلي من المراجع، مصدر المتن).
- `src/legalrag/`: `normalize.py` (+6 اختبارات ناجحة)، `ingest.py` (تقطيع بالمادة + تحقق بنيوي)، `evaluate.py` (لوحة النتائج)، `verify_refs.py` (فاحص الـ ground truth).
- **20 سؤال** في مجموعة التقييم (7 مباشر / 5 متعدد المواد / 3 عامية / 5 خارج المتن) — كلها `unverified`.

**مقيس:** لا شيء بعد. `python tasks.py eval` بيطبع: `20 questions loaded, 0 answered`.

**المعطّل (blocker):** المتن مش موجود. الـ PDF الرسمي لازم يُنزَّل يدوياً — الصفحات العامة بترجع ملخصات مش نصاً حرفياً (ADR-006). كل أرقام المواد في الـ 20 سؤال **غير محقَّقة** لحد ما ده يحصل.

**التالي (أسبوع 2 — 6 ساعات):**
1. تنزيل الـ PDF ← `data/raw/` وملء بطاقة المصدر في `data/raw/README.md` (15 دقيقة).
2. `python tasks.py ingest` وإصلاح مشاكل استخراج النص لحد ما التحقق البنيوي يعدّي (ساعتان).
3. فحص يدوي لخمس مواد مقابل الـ PDF (30 دقيقة).
4. `python tasks.py verify-refs` وتصحيح مراجع الأسئلة الغلط (ساعة).
5. الدفعة الثانية: 20 سؤال (val) (ساعتان).

**قرار مؤجل:** اختيار نموذج الـ embeddings — يتأجل لحد ما BM25 يبقى مقاساً، عشان يبقى فيه baseline يتقارن بيه.

---

## 2026-09-07 (لاحقاً نفس اليوم) — قرار المتن + صفحة الاختبار

**اتعمل:**
- **ADR-007**: مصدر المتن بقى `dataflare/egypt-legal-corpus` (HF، MIT، 2,434 قانون) بدل تنزيل PDF يدوي — ده شال المعطّل. المجموعة **مصدر نص خام مش متن**: بتعدّي على نفس المقسّم ونفس التحقق، وقانون واحد بس في M1.
- **ADR-008**: `fr3on/eg-legal-qa` **مرفوضة كمجموعة تقييم أساسية** (أسئلة مولّدة من نص المواد → دائرية، ومفيش فئة امتناع)، مقبولة كمجموعة ثانوية بعد التجميد لقياس الفجوة.
- `corpus_hf.py` + `tasks.py laws` + `ingest --law "<اسم>"`.
- `retrieve.py`: BM25 بدون تبعيات + Recall@k و MRR (بجزئية للأسئلة متعددة المواد).
- `server.py` + `ui/index.html`: صفحة اختبار محلية — تبويب أساسي بيشغّل مجموعة التقييم كلها ويوضّح الفاشل، وتبويبين للبحث الحر ومراجعة الأسئلة.

**مقيس:** لسه لا شيء حقيقي. اختبار ميكانيكي على متن صناعي من 4 مواد اشتغل من الطرف للطرف (search + eval + الصفحة).

**المعطّل:** تنزيل المجموعة لازم يتم من جهاز محمد — سياسة الشبكة في جلسة كلود بتحجب huggingface.co.

**قرار متوقّف على التنزيل:** لو قانون 151/2020 مش في المجموعة، الـ 20 سؤال **يتعادوا كتابة** على قانون موجود فعلاً. متستخدمهمش زي ما هم.

**التالي:** تنزيل ← `laws` ← `ingest --law` ← فحص أمانة 10 مواد ← `verify-refs` ← أول رقم BM25 حقيقي في `EVAL.md`.

---

## 2026-09-07 (الجلسة الثالثة) — تعديل مجموعة التقييم

**اتعمل:**
- **إعادة صياغة الـ 20 سؤال** بلغة المستخدم بدل ألفاظ المواد + إضافة `difficulty` + استبدال سؤال out_of_corpus كان قابلاً للتأويل.
- **ADR-009 — حارس التداخل اللفظي:** كل سؤال بيتقاس عليه `containment` (نسبة كلمات السؤال الموجودة في المادة). فوق الحد = السؤال بيكسب استرجاع مجاني؛ **تحت الحد الأدنى** = مفيش مرساة معجمية والـ BM25 بيتظلم. نطاق لكل فئة في `meta.json`.
- **ADR-010 — ربط المجموعة بالقانون:** `meta.corpus_law`، و`evaluate`/`verify-refs` بيرفضوا التشغيل (exit 3) لو المتن قانون تاني. من غيره، تشغيل أسئلة 151/2020 على قانون المرافعات بيطلع كنتيجة استرجاع ضعيفة ويتصلح في المكان الغلط.
- **ADR-011 — تقشير السوابق:** «البيانات»/«للبيانات»/«وبالبيانات» بقوا رمز واحد في تطبيع البحث فقط.
- 23 اختبار ناجح (كانوا 12).

**مقيس (على متن صناعي، مش نتيجة مشروع):** أثر التقشير على المقياس — سؤال منسوخ من المادة: 0.50 ← **0.88**. سؤال بصياغة المستخدم: 0.14 (ثابت). يعني المقياس كان بيقرا أقل من الحقيقة بالنص قبل التقشير.

**التوزيع الحالي للتداخل:** Q001 0.43 · Q002 0.17 · Q013 0.14 · Q014 0.12 — كلها داخل نطاق فئتها.

**المعطّل (لم يتغير):** تنزيل المجموعة من جهاز محمد، وتأكيد إن قانون 151/2020 موجود فيها. `corpus_law_status` لسه `TARGET_NOT_CONFIRMED`.

---

## 2026-09-08 — التراجع عن Hugging Face، الـ PDF رجع المصدر الوحيد

**اتعمل:**
- **ADR-012** يلغي ADR-007 ويرجّع ADR-006 ساري. اتشال بالكامل: `fetch.py`، `corpus_hf.py`، `tasks.py laws`، `tasks.py fetch-corpus`، وتبعيتَي `pyarrow` و`huggingface_hub`. مش fallback — إزالة.
- `ingest.py` بقى مسار واحد: `data/raw/*.pdf|*.txt` ← مواد. `--law` **إلزامي** في المسار ده كمان.
- **باج اتكشف في الطريق:** مسار الـ PDF القديم كان بيسيب `law_name` فاضي، و`binding_problem` بترجع `None` على قايمة فاضية — يعني حارس ADR-010 كان **ميت بصمت** في مسار الـ PDF. `--law` الإلزامي بيقفل ده، والاختبار `test_ingest_stamps_law_name_so_the_binding_guard_stays_armed` بيمنع رجوعه.
- **باج تاني اتكشف باختبار:** `data/raw/SOURCE.txt` (بطاقة المصدر) كان هيتقرا كنص متن لأنه `.txt`. اتستثنى بالاسم.
- `extraction_problems`: فحص لتلات طرق فشل استخراج العربي (مفيش طبقة نص / خط مش مربوط بـ Unicode / مفيش ترويسات مواد) — كلهم كانوا بيطلعوا «0 articles» وبيوجّهوا الإصلاح للمقسّم غلط. دلوقتي exit 2 برسالة تقول الحل.
- تحديث: `README.md`، `data/raw/README.md`، `SOURCE.txt`، `meta.json` notes، رسالة الـ UI، `Makefile.txt` (`LAW=`).

**مقيس:** **33 اختبار ناجح** (كانوا 23؛ الجداد 10 في `tests/test_pdf_source.py`). `python tasks.py eval` لسه بيشتغل من fresh clone: `20 questions loaded, 0 answered`.

**المعطّل (رجع بقصد):** المتن مش موجود لحد ما الـ PDF الرسمي يتنزّل يدوياً في `data/raw/`. ده الثمن المقبول في ADR-012 — رقم مبني على متن مش متحقق منه أسوأ من مفيش رقم. `corpus_law_status` لسه `TARGET_NOT_CONFIRMED`.

**التالي:**
1. تنزيل PDF قانون 151/2020 ← `data/raw/` + ملء `SOURCE.txt` (15 دقيقة).
2. `python tasks.py ingest --law "قانون حماية البيانات الشخصية"` — وتوقّع إن فحص الاستخراج يرسب أول مرة؛ الرسالة بتقول إيه الخطوة (OCR أو إعادة تصدير).
3. فحص أمانة 10 مواد مقابل الـ PDF ← `EVAL.md`.
4. `verify-refs` وتصحيح المراجع ← أول رقم BM25 حقيقي.

**بقايا على القرص:** `data/raw/egypt-legal-corpus/` (~24 MB parquet) لسه موجود. `ingest` مش بيقراه (البحث غير recursive). يتمسح وقت ما يحب محمد.

---

## 2026-09-08 (لاحقاً) — أول تشغيل حقيقي على PDF، وأول رقم

**اتعمل:**
- **المتن موجود.** قانون 151/2020، PDF ثنائي اللغة، **56 مادة** (7 إصدار + 49 قانون) و`structure OK`.
- **`pdf_text.py` (ADR-013):** `pdfplumber.extract_text()` مش صالح للعربي — بيرجّع ترتيب بصري، `البيانات` بتوصل `تانايبلا`، والتشغيل بينتهي «0 articles» فتدوّر على الغلط في المقسّم. إعادة بناء على مستوى الـ glyph. **6 عيوب اتلقت وكلها اتصلحت**، كل واحد بدليل مقيس — أخطرهم إن الأرقام العربية-الهندية (٠-٩) جوه نطاق يونيكود العربي فكانت بتتعكس: `مادة (٢٥)` ← `مادة (٥٢)`.
- **قاعدتين تقطيع جداد:** الترويسة لازم تشغل سطرها لوحدها (إحالة `المادة (21) من هذا القانون` كانت بتفتح مادة وهمية وتقطع المادة الحقيقية)، وإعادة بدء الترقيم بتفصل قانون الإصدار عن المرافق (الـ PDF بيرقّم مواد الإصدار بأرقام مش بترتيب).
- **`evaluate` بقى بيشغّل المسترجِع فعلاً** بدل لوحة ثابتة على صفر.
- `EVAL.md` — أول تشغيل موثّق. `meta.corpus_law_status` ← **CONFIRMED**.

**مقيس (حقيقي، مش صناعي):**
- **BM25 · k=5 · dev:** Recall@5 = **0.333** · MRR = **0.289** (15 سؤالاً مقيساً). بالفئة: direct 0.429 · multi_article 0.200 · colloquial 0.333.
- **`verify-refs`: 20/20**. مجموعة الأسئلة اتكتبت من مصادر ثانوية قبل وجود المتن — **18 مرجع صح من أول مرة**، والاتنين الباقيين رقم المادة فيهم صح كمان والغلط كان في الكلمة المفتاحية (القانون كاتب «اثنين وسبعين ساعة» بالحروف مش «72»). اتصححوا وموثقين.
- **59 اختبار ناجح** (كانوا 33).

**المعطّل الجديد (مسجّل في `SOURCE.txt` و`EVAL.md`):** النسخة **الرسمية** (الجريدة الرسمية عبر منشورات، 29 صفحة) **مسح ضوئي بصفر طبقة نص**. اللي اتستوعب نسخة معاد نشرها من طرف تالت فيها طبقة نص. **فحص الأمانة 10/10 لسه مفتوح** ومحتاج OCR (`ocrmypdf --language ara` مش متثبت). لحد ما يتعمل، الرقم فوق رقم عن الـ PDF ده مش عن القانون كما نُشر.

**قرار مطلوب من محمد:** 5 أسئلة تحت الحد الأدنى للتداخل اللفظي (Q002 · Q003 · Q007 · Q010 · Q012) — تتنقل لـ`colloquial` ولا يتضاف لها مرساة معجمية؟ قرار تصميم مجموعة.

**التالي:** OCR + فحص الأمانة · خط أساس كثيف للمقارنة بـ0.333 · الدفعة الثانية من الأسئلة (val).


---

## 2026-09-08 (لاحقاً) — الريبو اتعمل، وقرار الأسئلة اتحسم

**اتعمل:**
- **`git init`** — المشروع كان عايش على القرص من غير نسخة تانية. 3 كوميتات: الأساس، تتبّع `SOURCE.txt`، تثبيت LF. **A1 اتأكد فعلياً** (clone نظيف في مجلد مؤقت: `eval` بيخرج exit 0 و`pytest` 59 passed) — مش افتراض. **B3 في M2 بقى قابل للقياس** بعد ما كان مستحيل (معياره «مراجعة تاريخ الملفات في git»).
- **باج اتكشف في `.gitignore`:** `data/raw/*` كان بيستثني `SOURCE.txt` — وهي مش data، دي بطاقة المصدر اللي فيها الـ sha256 وعدّاد فحص الأمانة `__/10`. `EVAL.md` بيحيل عليها صراحةً، فالإحالة كانت **بتكسر في أي fresh clone** والبلوكر بتاع الأمانة كان بيختفي من الريبو بالكامل. اتتبعت.
- **بقايا HF اتمسحت** (24 MB parquet). `ingest` و`pytest` و`eval` كلهم اتشغّلوا بعدها — نفس الرقم بالظبط.
- **قرار الـ 5 أسئلة اتحسم (محمد): نقل لـ`colloquial`، مش إضافة مرساة معجمية.** Q002 · Q003 · Q007 · Q010 · Q012. `verify-refs --write` ← **20/20 verified**.

**مقيس:** الإجمالي **ما اتغيرش** (0.333 / 0.289 — نفس الأسئلة ونفس المسترجِع). اللي اتغير التوزيع، وده كان الكشف:

| | direct | multi_article | colloquial |
|---|---:|---:|---:|
| قبل | 0.429 (7) | 0.200 (5) | 0.333 (3) |
| بعد | **0.750** (4) | 0.333 (3) | **0.125** (8) |

أسئلة إعادة الصياغة كانت مبعثرة جوه `direct` فبتسحبه لتحت وبترفع `colloquial`، والرقمين كانوا متقاربين ومضلِّلين. بعد النقل: **BM25 شغّال لما فيه مرساة معجمية (0.750)، وبينهار على إعادة الصياغة (0.125)**. الفجوة ×6 — وده المقياس اللي النموذج الكثيف هيتحاكم عليه، مش الإجمالي.

**تكلفة النقل (مسجّلة في `EVAL.md`):** `direct` من 7 لـ4 وفقدت تغطية `law-8` و`law-17`؛ `multi_article` من 5 لـ3 (رقيقة جداً)؛ Q002/Q007 لهم توأمان عامّيان أصلاً (Q014/Q015) فبقى في تكرار جوه `colloquial`. الدفعة التانية لازم تعوّض الفئتين دول تحديداً.

**المعطّل (لم يتغير):** فحص الأمانة 10/10 — `ocrmypdf` و`tesseract` **مش متثبتين** (اتأكدت). لسه بيحجب نشر أي رقم.

**M1 دلوقتي:** A1 ✅ · A2 ✅ · A3 ✅ على الدفعة الأولى (20/60 سؤال، 100% verified) · A4 ❌ (1 من 4 تكوينات) · A5 ✅.

**التالي:** خط الأساس الكثيف (أكبر بند متبقٍ في A4) · الدفعة التانية 20 سؤال (val) بتركيز على `direct` و`multi_article` · OCR + فحص الأمانة قبل أي نشر.

---

## 2026-09-08 (الجلسة الرابعة) — خط الأساس الكثيف، والتكوينات الأربعة

**اتعمل:**
- **ADR-015: `intfloat/multilingual-e5-base`** — القرار المؤجل من أسبوع 1 اتحسم. مُدرَّب على الاسترجاع (غير متماثل) مش على التشابه العام. محلي، CPU، أوفلاين بعد التنزيل.
- **الجزء الأهم في ADR-015 مش النموذج — الحقل اللي بيدخله.** الكثيف بيضمّن `text` الحرفي، **مش `search_text`**. الفهرس المعجمي مطبَّع بعدوانية بقصد (ADR-004)، واللي BM25 بيشوفه تطبيعاً بيشوفه المحوّل تلفاً. والسؤال بيوصل النموذج **بلا `search_normalize`**. تلات أخطاء التوصيل دي (الحقل الغلط، إسقاط سابقة E5، تطبيع السؤال) **مبتطلّعش أي خطأ** — بتطلّع رقم كثيف منخفض معقول الشكل، فيتقرا «النماذج الكثيفة مش نافعة للعربي». تلاتتهم مثبّتين باختبارات بمشفّر بديل، بلا تنزيل.
- **ADR-016: RRF مش مجموع موزون.** درجات BM25 غير محدودة وجيب التمام في [-1,1]؛ الدمج بالقيمة محتاج وزن يتضبط على 15 سؤال = إفراط في المواءمة.
- **ADR-017: cross-encoder فوق أعلى 20 مرشح.** **باج اتكشف وأنا بكتب:** كنت مرّرت `Hit.snippet` (مقطوع 280 حرف) بدل النص الكامل — يعني كل مادة بعد فقرتها الأولى كانت هتتبتر، والحكم العملي في القانون غالباً في التانية. اتصلح واتقفل باختبار.
- `dense.py` (بصمة متن ضد الكاش البايت) · `fusion.py` · `rerank.py` · `ablate.py` · `tasks.py ablate`.
- **`ablate` ملف منفصل عن `eval` بقصد:** `eval` لازم يفضل شغّال من fresh clone بلا موديل وبلا torch. اتأكد: **0.3 ثانية**.

**مقيس (Run 2 — نفس الـ15 سؤال، نفس k):**

| التكوين | Recall@5 | MRR | ث/سؤال |
|---|---:|---:|---:|
| BM25 | 0.333 | 0.289 | 0.000 |
| **dense** | **0.767** | **0.588** | 0.107 |
| hybrid (RRF) | 0.600 | 0.600 | 0.138 |
| hybrid + rerank | 0.667 | 0.567 | 6.875 |

سقف المرشحين (recall@20 للهجين) = **0.933**.

**تلات قراءات، اتنين ضد التوقع:**
1. **الكثيف قفل الفجوة في مكانها بالظبط.** `colloquial` 0.125 ← **0.812**، و`direct` ثابتة 0.750. المكسب كله جه من مكان العجز. فحص يدوي: Q002 و Q007 (صفر تداخل) — BM25 خارج أعلى 5، الكثيف لقاهم (رتبة 3 و**1**). دول بالظبط السؤالين اللي ADR-014 رفض يضيفلهم مرساة معجمية؛ القرار ده هو اللي خلى النتيجة قابلة للقياس.
2. **الهجين أسوأ من الكثيف وحده** (0.600 مقابل 0.767). RRF بوزن متساوٍ بيسحب القوي لتحت لما يتدمج بضعيف. الأثر أوضح في `colloquial`: 0.812 ← 0.562، الدمج **أهدر تلت** المكسب.
3. **إعادة الترتيب بتنفع `direct` (1.000) و`multi_article` (0.833) وبتضرّ `colloquial`** (0.562 ← 0.438)، بتكلفة **×64 في الزمن** ونتيجة إجمالية أقل من الكثيف وحده. والسقف بيقول الباقي: المرشحين فيهم 0.933 والمعيد مستخرجش غير 0.667 — الفرق ده خسارة المعيد بإيده.

**الخلاصة:** الكثيف وحده هو الفائز — أعلى رقم، وأبسط تركيب، وأسرع زمن بعد BM25. **نتيجة سلبية عن الأنبوب الشائع، مقيسة لا مفترضة.**

**حدود القراءة (مكتوبة في `EVAL.md`):** 15 سؤالاً، و3 أسئلة بس في `multi_article` — الفرق بين 0.333 و0.833 هناك سؤالين. الفروق **اتجاه لا حجم**. المتن 56 مادة وأعلى 5 = 9% منه.

**مقيس كمان:** **84 اختبار ناجح** (كانوا 59؛ الجداد 25 في `tests/test_dense.py`). `eval` 0.3 ثانية. fresh clone لسه أخضر.

**المعطّل (لم يتغير):** فحص الأمانة 10/10 — OCR مش متثبت. الأرقام دي عن **الـ PDF ده** مش عن القانون كما نُشر.

**M1: A1 ✅ · A2 ✅ · A3 ✅ (20/60) · A4 ✅ · A5 ✅ — كل المعايير الخمسة مستوفاة على الدفعة الأولى.** الباقي للتسليم: 40 سؤال + فحص الأمانة.

**التالي:** الدفعة التانية 20 سؤال (val) بتركيز على `direct` و`multi_article` · وزن الهجين على val · OCR.

---

## 2026-09-08 (الجلسة الخامسة) — فحص الأمانة اتعمل، والنتيجة رسوب

**اتعمل:**
- **الجريدة الرسمية نُزِّلت** (‏6,470,641 بايت — مطابق للحجم الموثَّق، 29 صفحة، `sha256 deb05826…`). رابط التنزيل بيحتاج token من صفحة الـ node؛ الرابط المجرَّد بيرجّع 403 — مسجَّل في `SOURCE.txt`.
- **الادعاء «صفر طبقة نص» اتحقق:** 29 صورة، **0 حرف قابل للاستخراج**. المسح نفسه **نظيف جداً** (طباعة رقمية 1339×1916، مش تصوير رديء).
- **فحص الأمانة على 11 مادة — ADR-018.**

**النتيجة: رسوب. 9 من 11 مادة مختلفة عن الجريدة.** المطابقتان م11 و م12.

| | الجريدة | المُستوعَب |
|---|---|---|
| **م7** | **«اثنتين وسبعين ساعة»** | **«اثنين وسبعين ساعة»** |
| م7 | «بموافاة المركز» | «بموافاته» |
| م1 | «بالكلمات والعبارات» | «بالمصطلحات» |
| م6 | «أى من الحالات» / «عقد لصالح» | «إحدى الحالات» / «عقد الصالح» |
| م10 | «أن يكون» | «أن تكون» |
| م17 | «الإشارة إلى أن» | «الإشارة بأن» |
| م18 | «إلكترونى» | «إليكتروني» ×3 |
| م3 | «التنفيذية لهذا القانون» | «لهذا القانون» محذوفة |
| م5 | «هذا القانون» | «هذا القنون» |

**الدليل إن المصدر هو السبب مش كودنا:** «اثنتين» بترد **صفر مرة** في النص المستخرج من نسخة privacylaws؛ «بالمصطلحات» مرة و«بالكلمات» صفر. كلمات مختلفة بالكامل مش بتيجي من باج مسافات. **النسخة المعاد نشرها نص آخر، مش نسخة من النص.**

**أثر على المجموعة (فعل مؤجَّل بشرط):** `Q001` و`Q013` كلمتهم المفتاحية «اثنين وسبعين» **مش موجودة في القانون كما نُشر**. رقم المادة (law-7) **صح في المصدرين**. `verify-refs` بيمرّ النهاردة لأنه بيقيس مقابل المتن المُستوعَب؛ عند إعادة الاستيعاب هيسقطوا — **وده السلوك الصح**. متتغيرش دلوقتي.

**أثر على كل رقم:** Run 1 و Run 2 **صالحين كمقارنة استرجاع** (التكوينات الأربعة شافت نفس المتن بالظبط، فـ0.767 مقابل 0.333 نتيجة حقيقية) و**غير صالحين كقول عن قانون 151/2020**.

- **ADR-013 ادّعى «0 كلمات مكسورة متبقية» — الادعاء خاطئ.** `broken_words.py` + `tasks.py broken-words`: **20 حالة في 13 مادة**. المعيار قابل لإعادة التشغيل (الشكل الملتحم يرد في المتن كتوكن مستقل) بدل ادعاء في وثيقة. المنشأ **غير محسوم ولم يُنشر تقسيم** — فحصان تعارضا، وعكس الترتيب البصري ساذجاً غير موثوق مع روابط العربية. الثابت إن الكسور موجودة و**بتخفض 0.333 بمقدار غير مقيس**.

- **OCR جُرِّب ولم يُعتمد — ADR-019.** `paddlepaddle` **بلا wheel لبايثون 3.14** رغم إن أوزان PaddleOCR العربية في الكاش (تصحيح لتقديري في الجلسة اللي فاتت). `easyocr` اشتغل: 83–99 ث/صفحة. **شهد لصالح الجريدة بشكل مستقل** — قرأ «ببياناته» و«عشرين ألف جنيه» فأكّد قراءتين بصريتين من مسار تاني. **لكنه بيخرّب الأرقام** (`١٥`←`٥`، `٢٠٢٠`←`٢٠٢`) وبيسقّط الميم في «الـم». الأرقام هي بالظبط اللي المتن القانوني محتاجه. المرشح التالي: `surya-ocr`، يُقاس على دقة الأرقام أولاً.

**مقيس:** **92 اختبار ناجح** (كانوا 84؛ الجداد 8 في `tests/test_broken_words.py`). `eval` و`ablate` بيرجّعوا نفس الأرقام.

**M1:** الخمسة معايير لسه مستوفاة على الدفعة الأولى — **بس A5 (كل رقم مصحوب بقيوده) بقى له قيد أثقل بكتير**: قيد المصدر دلوقتي مقيس لا مفترض.

**التالي:** `surya-ocr` واختبار دقة الأرقام · إعادة استيعاب من الجريدة لو نجح · تصحيح Q001/Q013 بعدها · الدفعة التانية 20 سؤال.

---

## 2026-09-09 (الجلسة السادسة) — بوابة الأرقام، وأربعة تكوينات OCR مقيسة

**اتعمل:**
- **ADR-020 — بوابة دقة الأرقام بقت كود.** ADR-019 كتب الشرط («أي محرك يُقاس على دقة الأرقام في صفحة معروفة أولاً») وسابه جملة في وثيقة — نفس شكل ادعاء ADR-013 قبل ما يتضح خطؤه. دلوقتي: `python tasks.py ocr-gate <json>` + حقيقة أرضية في `evals/ocr/digit_ground_truth.json` **مقروءة من صور الصفحات بالعين** (محرّك مش مسطرة لنفسه). تلات صفحات (7 · 10 · 28)، **42 رمزاً**، والعتبة 99%.
- **ليه الأرقام مش الحروف:** الجريدة طباعة نظيفة والمحرّكات بتقرا النثر مقبول. النثر مش اللي بينكسر. حرف غلط جوه جملة أي قارئ بيشوفه؛ **رقم** غلط محدش بيشوفه، لأن «المادة (١٢)» و«المادة (١٢١)» سليمتين شكلاً. أرقام المواد هي **المفتاح الأساسي**: بتقطّع المتن، وهي ground truth كل سؤال، وكل إحالة بتُحلّ من خلالها.
- **`src/legalrag/ocr_text.py`** — تحويل مخرجات OCR لمتن جاهز للاستيعاب: إزالة وسوم HTML (surya بيخرج `<b>مادة</b>`)، وإزالة **أثاث الصفحات** (الترويسة الجارية + رقم الصفحة — لو دخلوا، 29 مادة بتكتسب جملة مش بتاعتها و BM25 بيشوف نفس الترويسة 29 مرة)، وترتيب الصفحات **عددياً** (`p10` بيسبق `p2` كنص).

**مقيس — أربعة تكوينات، نفس التلات صفحات:**

| المحرّك | دقة الأرقام | الحالات الحرجة | ث/صفحة |
|---|---:|:--:|---:|
| easyocr 1.7.2 | 71.4% | 0/3 | 83–99 |
| PaddleOCR · `server_det` | 21.4% | 0/3 | 590–669 |
| PaddleOCR · `mobile_det` | 16.7% | 0/3 | 75–119 |
| **surya-ocr 0.17.1** | **92.9%** | **3/3 ✓** | 160–198 |

**الأنماط مختلفة، وده الجزء المفيد.** easyocr **بيشوّه** (`المادة (١٢)`←`١٢١`، والفاصلة العربية بتتقرا رقم) — صامت وبيخترع مواد مش موجودة. Paddle **بيحذف** (`مادة (٤)`←`مادة`) — نثره العربي أنضف من easyocr بوضوح، بس بلا أرقام مفيش حاجة تتقطّع؛ بيفشل بصوت عالي وده أحسن، وغير صالح بنفس القدر.

**تلات فرضيات اتّختبرت، تلاتتهم سقطت:**
1. «عيب paddle في نموذج الكشف» — تبديل الكشف ما حسّنش (21.4%←16.7%)، وبإزالة الكشف تماماً وتغذية قصاصات مقصوصة بإيدي: سطر الترويسة اللي فيه **أربعة** أرقام رجع بلا أي رقم والكلمات العربية سليمة. **`arabic_PP-OCRv5_mobile_rec` أعمى عن الأرقام العربية-الهندية.**
2. «الدقة هي قيد surya» — تكبير ×2 LANCZOS كـ`highres_images`: النتيجة **ما اتغيرتش**، والأخطاء **اتنقلت** لحد ما `مادة (٤٨)` بقت `١٨` — ترويسة مادة فسدت بعد ما كانت صح. الحد هو المسح (1339×1916 @ 72 ppi)، و1× أفضل تكوين.
3. «`ingest` جاهز لنص OCR» — لأ. تلات أعطال صامتة، كلها في `ocr_text.py` دلوقتي بعشر اختبارات.

**شاهد ثالث مستقل على فحص الأمانة:** surya — بمعمارية مختلفة كلياً عن easyocr — أعاد إنتاج **كل** نتيجة بصفر تداخل: «اثنتين» ×2 · «بموافاة» · «لصالح» · «الإلكترونية» · «بالكلمات والعبارات». وقريت صفحة 10 بعيني وأكّدت العبارات من المصدر مباشرةً. بقى فيه تلات شهود: القراءة البصرية، easyocr، surya.

**تصحيحان واقعيان لـ ADR-019:** (أ) قال إن مسار paddle مقفول لغياب wheel لبايثون 3.14 — صح عن بيئة المشروع، **غلط كاستنتاج**: الحزمة والأوزان العربية كانت مثبتة أصلاً في بيئة Python 3.12 على نفس الجهاز. المسار كان مفتوح، بس مش بيودّي لحاجة. (ب) «surya له wheel لـ3.14» مضلِّل — الـwheel `py3-none-any` لكن قيده `pillow<11` **غير قابل للإشباع** على 3.14، وتثبيته في بيئة المشروع كان هينزّل Pillow من 12.3.0 لـ10.4.0 من تحت easyocr و sentence-transformers. اتعمل في **بيئة conda معزولة**.

**مقيس كمان:** **112 اختبار ناجح** (كانوا 92؛ الجداد 10 في `test_ocr_gate.py` و10 في `test_ocr_text.py`). بيئة المشروع **ما اتأثرتش**: pillow 12.3.0، `eval` 0.333/0.289 نفس الرقم.

**المعطّل (لم يتغير، لكن بقى مقيساً):** مفيش متن أمين. surya راسب في العتبة (92.9% < 99%) — بس أخطاؤه التلاتة كلها في ص28 (رقم هامشي + ذيل قائمة)، و**كل ترويسات المواد صح**.

**التالي:** تشغيل surya على الـ29 صفحة (شغّال) ← `ocr-to-raw` ← **التحقق البنيوي في `ingest`** (تسلسل 56 مادة بلا فجوة ولا تكرار) — وده فحص لأرقام المواد **أقوى من أي عيّنة**، لأن أي قراءة غلط بتكسر التسلسل. بعده مقارنة مادة بمادة مع المتن الحالي **قبل** أي قرار إعادة استيعاب. ثم Q001/Q013، ثم LLM التوليد (M2).

---

## 2026-09-10 (الجلسة السابعة) — متن الجريدة قيس ولم يُعتمد، وأول أرقام توليد

**اتعمل — المتن:**
- **surya على الـ29 صفحة كلها** ← `ocr-to-raw` ← استيعاب في **نسخة معزولة** (المتن الحالي ما اتلمسش ولا مرة).
- **باجان في المقسّم اتكشفوا بالتشغيل** (ADR-020): الجريدة بتمطّط الحروف بالتطويل (`مــادة (١٥)`) والنسخة المعاد نشرها لأ — فترويسة ما بتتلقطش **ما بتطلّعش خطأ**، بتلحم مادتين في واحدة. والجريدة بتحط ترويسات الإصدار بين قوسين `(المادة الأولى)`. **فحص الانحدار:** إعادة استيعاب النسخة الأصلية بالمقسّم المصحَّح بتطلّع ملف **مطابق بايت ببايت** — 56 مادة، 7+49، `structure OK`.
- **باج الأرقام في موديولاتي الجديدة:** العربية فيها تلات كتل ترقيم، وsurya بيخرج اتنين منها (`٠-٩` و`۰-۹` الممتدة) **وأحياناً جوه الرقم الواحد** (`مادة ( ۲٤ )`). كود المشروع القديم كان صح؛ الجديد بتاعي لأ. **ما غيّرش أي رقم منشور** (الصفحات المقيسة كلها الكتلة الأولى) بس خلّى قراءتي الأولى للترويسات غلط.

**مقيس — متن الجريدة مقابل المتن الحالي (ADR-021):**

| | |
|---|---|
| انقسم إلى | **56 مادة: 7 إصدار + 49 قانون** — البنية المنشورة بالظبط |
| تشابه `search_normalize` | **0.988** |
| تشابه حرفي | 0.973 |
| ≥0.90 | **55/56** |
| أرقام ترويسات قراها OCR غلط | 3 — كلها قابلة للكشف من موضعها |

المادة الوحيدة تحت 0.90 هي `issuance-7`، **والفارق لصالح الجريدة**: فيها بيان التصديق الرئاسي المحذوف من النسخة المعاد نشرها. وكسر تاني اتحسم: `مض ي` عندنا مقابل `مضى` سليمة في الجريدة.

**قرار محمد: المتن ما يتبدلش.** التصحيح من الموضع تدخّل بشري في نص السجل، وبتشابه 0.988 الأرقام مش هتتحرك — فالمشترى ترقية في **الادعاء** لا في **القياس**. Q001/Q013 زي ما هما، والقيد ساري.

**اتعمل — التوليد (M2):**
- `cite.py` + `generate.py` + `answer_eval.py` + اختباراتهم **اتثبتوا والنموذج لسه بينزّل** (`8f1694b`) — القاعدة إن الـeval يسبق اللي بيحكم عليه، وتاريخ git هو الدليل.
- `llama-cpp-python` **بلا wheel لـ3.14 إطلاقاً** و Ollama مش متثبت ← transformers على CPU (ADR-022).

**مقيس — Run 3:**

```
B1  مسنَدة بالكامل  2/14 = 14.3%   ❌     B2  امتنع على out_of_corpus  5/5 = 100%   ✅
    مادة مخترعة     0                        امتنع على القابل للإجابة  1/15 = 6.7%
    مادة ما اتسترجعتش 0
    بلا سند         12                       صيغة [مادة N]            0/14 = 0%
```

**النموذج مش بيكذب — بس مش بيستشهد.** صفر اختلاق وصفر استشهاد بمادة ما اتسترجعتش، وصفر التزام بالصيغة. السلوك الغالب **نسخ نص المادة بدل الإجابة**.

**تلات أخطاء في القياس نفسه، كلها اتكشفت بالتشغيل:**
1. **`dtype="auto"` مع 3B: ست ساعات لإجابة واحدة** (bfloat16 على CPU بلا دعم أصلي). float32: أسرع ~1400×. **العلّة الـdtype مش الحجم.** و`device_map="cpu"` الغلط **طلّع خطأ فاتصلح في ثواني**؛ الـdtype **ما طلّعش حاجة** — شكله «معلّق» مش «إعداد غلط».
2. **B1 كان بيمرّر إجابات ما بتستشهدش بحاجة** — طبع `PASS` على 12 إجابة بلا سند. نفس الفخ اللي صمّمت ضده في B2 بسطرين تحته؛ الفرق إن B2 كان محمي باختبار.
3. **الاستشهاد جوه نص منسوخ مش استشهاد** — القانون بيحيل على نفسه. صحّح رقماً منشوراً من 21.4% لـ**14.3%**.

**حد مسجَّل لا مُغطّى:** Q002 استشهدت بالمادة 7 (الإبلاغ عن الاختراق) لدعوى عن تعيين مسؤول خصوصية — **مسنَد ميكانيكياً وخاطئ موضوعياً**. الفحص بيتأكد إن الاستشهاد يُحَل وكان ضمن المسترجَع، مش إنه **يسند الجملة**.

**مقيس كمان:** **148 اختبار ناجح** (كانوا 92 أول الجلسة السادسة). `runs/answer_eval.json` بيحفظ كل إجابة، و`answer-eval --report-only` بيعيد التدقيق منها **بلا تحميل نموذج** — لأن الفحص غلط مرتين والإجابات كانت سليمة في المرتين.

**التالي:** B3–B5 (tool-use · workflow مقابل agent · Error Analysis) · الدفعة التانية 20 سؤال (val) · نموذج أكبر لو اتوفّر runtime أسرع — عندها السؤال «رسوب B1 خاصية النموذج ولا خاصية الحجم؟» يبقى قابلاً للإجابة.
