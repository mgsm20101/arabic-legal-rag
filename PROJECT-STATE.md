# PROJECT-STATE

سطر واحد أسبوعياً. الغرض: استئناف الشغل بعد انقطاع من غير إعادة بناء السياق في الدماغ.

---

## 2026-09-07 — أسبوع 1

**اتعمل:**
- هيكل الريبو + `python tasks.py eval` بيشتغل من fresh clone (المعيار المتفق عليه للأسبوع الأول ✅).
- `PRD.md` v1.0 — النطاق، تقسيم dev/val/locked، معايير قبول M1 و M2 بأرقام.
- `DECISIONS.md` — 6 ADRs (eval-first، التقطيع بالمادة، متن واحد، فصل التطبيع، التحقق الآلي من المراجع، مصدر المتن).
- `src/legalrag/`: `normalize.py` (+6 اختبارات ناجحة)، `ingest.py` (تقطيع بالمادة + تحقق بنيوي)، `evaluate.py` (لوحة النتائج)، `verify_refs.py` (فاحص الـ ground truth).
- **20 سؤال** في مجموعة التقييم (7 مباشر / 5 متعدد المواد / 3 عامية / 5 خارج المتن) — كلها `unverified`.

**مقيس:** لا شيء بعد. `python tasks.py eval` بيطبع: `20 questions loaded, 0 answered`.

**المعطّل (blocker):** المتن مش موجود. الـ PDF الرسمي لازم يُنزَّل يدوياً — الصفحات العامة بترجع ملخصات مش نصاً حرفياً (ADR-006). كل أرقام المواد في الـ 20 سؤال **غير محقَّقة** لحد ما ده يحصل.

**التالي (أسبوع 2 — 6 ساعات):**
1. تنزيل الـ PDF ← `data/raw/` وملء بطاقة المصدر في `data/raw/README.md` (15 دقيقة).
2. `python tasks.py ingest` وإصلاح مشاكل استخراج النص لحد ما التحقق البنيوي يعدّي (ساعتان).
3. فحص يدوي لخمس مواد مقابل الـ PDF (30 دقيقة).
4. `python tasks.py verify-refs` وتصحيح مراجع الأسئلة الغلط (ساعة).
5. الدفعة الثانية: 20 سؤال (val) (ساعتان).

**قرار مؤجل:** اختيار نموذج الـ embeddings — يتأجل لحد ما BM25 يبقى مقاساً، عشان يبقى فيه baseline يتقارن بيه.

---

## 2026-09-07 (لاحقاً نفس اليوم) — قرار المتن + صفحة الاختبار

**اتعمل:**
- **ADR-007**: مصدر المتن بقى `dataflare/egypt-legal-corpus` (HF، MIT، 2,434 قانون) بدل تنزيل PDF يدوي — ده شال المعطّل. المجموعة **مصدر نص خام مش متن**: بتعدّي على نفس المقسّم ونفس التحقق، وقانون واحد بس في M1.
- **ADR-008**: `fr3on/eg-legal-qa` **مرفوضة كمجموعة تقييم أساسية** (أسئلة مولّدة من نص المواد → دائرية، ومفيش فئة امتناع)، مقبولة كمجموعة ثانوية بعد التجميد لقياس الفجوة.
- `corpus_hf.py` + `tasks.py laws` + `ingest --law "<اسم>"`.
- `retrieve.py`: BM25 بدون تبعيات + Recall@k و MRR (بجزئية للأسئلة متعددة المواد).
- `server.py` + `ui/index.html`: صفحة اختبار محلية — تبويب أساسي بيشغّل مجموعة التقييم كلها ويوضّح الفاشل، وتبويبين للبحث الحر ومراجعة الأسئلة.

**مقيس:** لسه لا شيء حقيقي. اختبار ميكانيكي على متن صناعي من 4 مواد اشتغل من الطرف للطرف (search + eval + الصفحة).

**المعطّل:** تنزيل المجموعة لازم يتم من جهاز محمد — سياسة الشبكة في جلسة كلود بتحجب huggingface.co.

**قرار متوقّف على التنزيل:** لو قانون 151/2020 مش في المجموعة، الـ 20 سؤال **يتعادوا كتابة** على قانون موجود فعلاً. متستخدمهمش زي ما هم.

**التالي:** تنزيل ← `laws` ← `ingest --law` ← فحص أمانة 10 مواد ← `verify-refs` ← أول رقم BM25 حقيقي في `EVAL.md`.

---

## 2026-09-07 (الجلسة الثالثة) — تعديل مجموعة التقييم

**اتعمل:**
- **إعادة صياغة الـ 20 سؤال** بلغة المستخدم بدل ألفاظ المواد + إضافة `difficulty` + استبدال سؤال out_of_corpus كان قابلاً للتأويل.
- **ADR-009 — حارس التداخل اللفظي:** كل سؤال بيتقاس عليه `containment` (نسبة كلمات السؤال الموجودة في المادة). فوق الحد = السؤال بيكسب استرجاع مجاني؛ **تحت الحد الأدنى** = مفيش مرساة معجمية والـ BM25 بيتظلم. نطاق لكل فئة في `meta.json`.
- **ADR-010 — ربط المجموعة بالقانون:** `meta.corpus_law`، و`evaluate`/`verify-refs` بيرفضوا التشغيل (exit 3) لو المتن قانون تاني. من غيره، تشغيل أسئلة 151/2020 على قانون المرافعات بيطلع كنتيجة استرجاع ضعيفة ويتصلح في المكان الغلط.
- **ADR-011 — تقشير السوابق:** «البيانات»/«للبيانات»/«وبالبيانات» بقوا رمز واحد في تطبيع البحث فقط.
- 23 اختبار ناجح (كانوا 12).

**مقيس (على متن صناعي، مش نتيجة مشروع):** أثر التقشير على المقياس — سؤال منسوخ من المادة: 0.50 ← **0.88**. سؤال بصياغة المستخدم: 0.14 (ثابت). يعني المقياس كان بيقرا أقل من الحقيقة بالنص قبل التقشير.

**التوزيع الحالي للتداخل:** Q001 0.43 · Q002 0.17 · Q013 0.14 · Q014 0.12 — كلها داخل نطاق فئتها.

**المعطّل (لم يتغير):** تنزيل المجموعة من جهاز محمد، وتأكيد إن قانون 151/2020 موجود فيها. `corpus_law_status` لسه `TARGET_NOT_CONFIRMED`.

---

## 2026-09-08 — التراجع عن Hugging Face، الـ PDF رجع المصدر الوحيد

**اتعمل:**
- **ADR-012** يلغي ADR-007 ويرجّع ADR-006 ساري. اتشال بالكامل: `fetch.py`، `corpus_hf.py`، `tasks.py laws`، `tasks.py fetch-corpus`، وتبعيتَي `pyarrow` و`huggingface_hub`. مش fallback — إزالة.
- `ingest.py` بقى مسار واحد: `data/raw/*.pdf|*.txt` ← مواد. `--law` **إلزامي** في المسار ده كمان.
- **باج اتكشف في الطريق:** مسار الـ PDF القديم كان بيسيب `law_name` فاضي، و`binding_problem` بترجع `None` على قايمة فاضية — يعني حارس ADR-010 كان **ميت بصمت** في مسار الـ PDF. `--law` الإلزامي بيقفل ده، والاختبار `test_ingest_stamps_law_name_so_the_binding_guard_stays_armed` بيمنع رجوعه.
- **باج تاني اتكشف باختبار:** `data/raw/SOURCE.txt` (بطاقة المصدر) كان هيتقرا كنص متن لأنه `.txt`. اتستثنى بالاسم.
- `extraction_problems`: فحص لتلات طرق فشل استخراج العربي (مفيش طبقة نص / خط مش مربوط بـ Unicode / مفيش ترويسات مواد) — كلهم كانوا بيطلعوا «0 articles» وبيوجّهوا الإصلاح للمقسّم غلط. دلوقتي exit 2 برسالة تقول الحل.
- تحديث: `README.md`، `data/raw/README.md`، `SOURCE.txt`، `meta.json` notes، رسالة الـ UI، `Makefile.txt` (`LAW=`).

**مقيس:** **33 اختبار ناجح** (كانوا 23؛ الجداد 10 في `tests/test_pdf_source.py`). `python tasks.py eval` لسه بيشتغل من fresh clone: `20 questions loaded, 0 answered`.

**المعطّل (رجع بقصد):** المتن مش موجود لحد ما الـ PDF الرسمي يتنزّل يدوياً في `data/raw/`. ده الثمن المقبول في ADR-012 — رقم مبني على متن مش متحقق منه أسوأ من مفيش رقم. `corpus_law_status` لسه `TARGET_NOT_CONFIRMED`.

**التالي:**
1. تنزيل PDF قانون 151/2020 ← `data/raw/` + ملء `SOURCE.txt` (15 دقيقة).
2. `python tasks.py ingest --law "قانون حماية البيانات الشخصية"` — وتوقّع إن فحص الاستخراج يرسب أول مرة؛ الرسالة بتقول إيه الخطوة (OCR أو إعادة تصدير).
3. فحص أمانة 10 مواد مقابل الـ PDF ← `EVAL.md`.
4. `verify-refs` وتصحيح المراجع ← أول رقم BM25 حقيقي.

**بقايا على القرص:** `data/raw/egypt-legal-corpus/` (~24 MB parquet) لسه موجود. `ingest` مش بيقراه (البحث غير recursive). يتمسح وقت ما يحب محمد.

---

## 2026-09-08 (لاحقاً) — أول تشغيل حقيقي على PDF، وأول رقم

**اتعمل:**
- **المتن موجود.** قانون 151/2020، PDF ثنائي اللغة، **56 مادة** (7 إصدار + 49 قانون) و`structure OK`.
- **`pdf_text.py` (ADR-013):** `pdfplumber.extract_text()` مش صالح للعربي — بيرجّع ترتيب بصري، `البيانات` بتوصل `تانايبلا`، والتشغيل بينتهي «0 articles» فتدوّر على الغلط في المقسّم. إعادة بناء على مستوى الـ glyph. **6 عيوب اتلقت وكلها اتصلحت**، كل واحد بدليل مقيس — أخطرهم إن الأرقام العربية-الهندية (٠-٩) جوه نطاق يونيكود العربي فكانت بتتعكس: `مادة (٢٥)` ← `مادة (٥٢)`.
- **قاعدتين تقطيع جداد:** الترويسة لازم تشغل سطرها لوحدها (إحالة `المادة (21) من هذا القانون` كانت بتفتح مادة وهمية وتقطع المادة الحقيقية)، وإعادة بدء الترقيم بتفصل قانون الإصدار عن المرافق (الـ PDF بيرقّم مواد الإصدار بأرقام مش بترتيب).
- **`evaluate` بقى بيشغّل المسترجِع فعلاً** بدل لوحة ثابتة على صفر.
- `EVAL.md` — أول تشغيل موثّق. `meta.corpus_law_status` ← **CONFIRMED**.

**مقيس (حقيقي، مش صناعي):**
- **BM25 · k=5 · dev:** Recall@5 = **0.333** · MRR = **0.289** (15 سؤالاً مقيساً). بالفئة: direct 0.429 · multi_article 0.200 · colloquial 0.333.
- **`verify-refs`: 20/20**. مجموعة الأسئلة اتكتبت من مصادر ثانوية قبل وجود المتن — **18 مرجع صح من أول مرة**، والاتنين الباقيين رقم المادة فيهم صح كمان والغلط كان في الكلمة المفتاحية (القانون كاتب «اثنين وسبعين ساعة» بالحروف مش «72»). اتصححوا وموثقين.
- **59 اختبار ناجح** (كانوا 33).

**المعطّل الجديد (مسجّل في `SOURCE.txt` و`EVAL.md`):** النسخة **الرسمية** (الجريدة الرسمية عبر منشورات، 29 صفحة) **مسح ضوئي بصفر طبقة نص**. اللي اتستوعب نسخة معاد نشرها من طرف تالت فيها طبقة نص. **فحص الأمانة 10/10 لسه مفتوح** ومحتاج OCR (`ocrmypdf --language ara` مش متثبت). لحد ما يتعمل، الرقم فوق رقم عن الـ PDF ده مش عن القانون كما نُشر.

**قرار مطلوب من محمد:** 5 أسئلة تحت الحد الأدنى للتداخل اللفظي (Q002 · Q003 · Q007 · Q010 · Q012) — تتنقل لـ`colloquial` ولا يتضاف لها مرساة معجمية؟ قرار تصميم مجموعة.

**التالي:** OCR + فحص الأمانة · خط أساس كثيف للمقارنة بـ0.333 · الدفعة الثانية من الأسئلة (val).

