# M1 (retrieval benchmark) — kept deliberately small.
# Nothing here is needed to run `python tasks.py eval` on the question set alone.
pdfplumber>=0.11        # read the official law PDF (ADR-006/012)
rank-bm25>=0.2.2        # lexical retrieval baseline
sentence-transformers>=3.0   # dense baseline + cross-encoder reranker (ADR-015/017)
numpy>=1.24             # embedding matrix; `eval` does not need it, `ablate` does
# pytest is in requirements-dev.txt: this file is what the image installs.

# P1 demo layer (ADR-023) — local upload + chat. Generation talks to a local
# Ollama server over HTTP, so no model runtime is pinned here.
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9   # multipart form parsing for uploads
httpx>=0.27               # Ollama client, and FastAPI's TestClient
